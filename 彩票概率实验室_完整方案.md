# 彩票数据 / AI 概率实验室 —— 完整产品与技术方案

> 综合你自己的分析、Gemini 的方案、以及你对 Gemini 方案的修正，整理成的一份可直接执行的完整方案。

---

## 一、项目定位（一切设计的前提）

### 1.1 核心定位

**不做**：AI/算法预测彩票中奖号码的工具。
**做**：一个用真实开奖数据，持续检验"选号策略/算法/AI 是否真的比随机数强"的公开实验平台。

一句话定位：

> **"随机数打脸实验室"——用 1000+ 期真实数据，让所有选号玄学、统计策略、机器学习模型，接受同一个残酷的对手：大数定律。**

**项目北极星原则（写进方法论页面，所有排行榜条目都要遵守）：**

> **任何策略，在没有通过盲测之前，都不准自称"有效"。**

即便某个模型连续很多期表现亮眼，展示时也要同时给出"历史开发集表现"和"盲测集表现"两个数字，并明确标注当前证据是否足以支持"优于随机"这个结论——不够就写"不够"，不要因为好看就含糊过去。

### 1.2 命名与文案原则

- 避免使用"预测""稳赚""精准""内部号码""VIP"等词。
- 建议命名方向：`XX概率实验室` / `XX随机数实验室` / `算法擂台` / `Number Lab`，不要出现"预测网"字样。
- 首页必须有清晰声明：本项目为统计与算法科普实验，不提供售彩、代购、充值、返利等服务；彩票开奖为独立随机事件，历史数据不影响未来结果。

### 1.3 合规边界（写死在产品规则里，不要触碰）

- 不接入下注、代购、充值、拼团购彩、返利。
- 不承诺、不暗示、不使用中奖话术（"高命中率""专家推荐""保底"等）。
- 国内对互联网售彩管控严格，未经批准不得通过互联网销售彩票；官方也多次提示"AI预测彩票号码"没有科学依据。产品必须始终停留在"数据分析 + 算法科普"层面。
- **生成的号码、资金曲线旁绝对不能出现"立即购买""一键下单""复制号码去投注"这类交互按钮**——一旦出现，产品性质就从"实验"滑向"售彩导流"，这是最容易被忽视但最不能碰的一条线。

---

## 二、产品形态：网页 vs Agent Skill，怎么选

**结论：先做静态网页，Skill 作为第二入口后置接入，不要一开始就做 Skill，也不要做"纯随机点击生成"的网页。**

原因：

| 维度 | 纯随机静态页 | 每次现算的 Agent Skill | 推荐方案：算法引擎 + 回测 + 网页（AI 后置） |
|---|---|---|---|
| 可复现性 | 有，但无意义 | 差（每次结果可能不同） | 好（同数据+同算法=同结果） |
| 成本 | 极低 | 每次调用都要花 token/等待 | 低（AI 只在开奖后跑一次批处理） |
| 内容/传播价值 | 低，同质化严重 | 中 | 高（回测曲线、排行榜天然适合传播） |
| 技术含量体现 | 低 | 中 | 高（数据处理+统计+回测+可选ML） |
| 用户回访理由 | 几乎没有 | 一般 | 强（下一期开奖→验证上期预测→更新排行榜，天然循环） |

原则：**算法负责计算（确定性、可复现），AI 只负责"解释结果"和"生成战报"，不负责"决定号码"。** Skill 之后可以作为"用自然语言查询当前排行榜/生成个性化策略解读"的交互层加上去。

---

## 三、数学基础（必须先想清楚，否则后面的"回测""排行榜"都是假的）

这是整个项目的地基，比 UI 和 AI 都重要。

### 3.1 期望值是固定的，这是产品最大的看点而不是缺陷

以双色球为例：红球 33 选 6，任意一组固定选号，在开奖真正均匀随机的前提下：

```
单期红球命中期望 E = 6 × (6/33) ≈ 1.0909 个
```

大乐透前区 35 选 5：

```
单期前区命中期望 E = 5 × (5/35) ≈ 0.7143 个
```

**结论**：在"开奖机制公平、每期独立同分布"这个模型假设下，只要选号策略在开奖前就已确定、不使用未来信息，那么无论用随机、冷热、遗漏、贝叶斯、XGBoost 还是 LLM，长期平均命中数理论上都会趋近同一个期望值。**这不是产品的 bug，而是产品最核心的科普内容**——"用真实数据持续检验，当前样本是否呈现出与这个模型明显不一致的证据"。注意文案上不要写成"彩票已经被数学证明完全随机"这种绝对化断言：我们能验证的是模型假设下各策略期望是否一致，不是对开奖机制本身下结论。这个措辞收紧看起来学究，但恰恰是这类项目"讲科学"人设最容易被挑理的地方。

### 3.2 不要说"回测 300 期就一定收敛"——要展示置信区间

有限样本下，即使长期期望相同，短期波动依然可能很大。正确做法是把"收敛过程"和"波动区间"都做成可视化内容，而不是简单地宣称"都一样"。

给每个策略配置：

```
平均命中数 = 样本均值
理论期望值 = 1.0909（红球）/ 0.7143（前区）等
95% 置信区间 = 样本均值 ± 1.96 × (样本标准差 / √期数)
```

页面上直接展示："当前 XX 策略平均命中 1.094，高于理论期望 0.003，但差异没有统计显著性（落在 95% 置信区间内）"。这比单纯甩一个数字有说服力得多，也更真实。

### 3.3 回测必须是 Walk-Forward，禁止数据泄漏

**错误做法**（有数据泄漏风险）：

```
用全部 1000 期历史数据 → 计算热号/冷号/权重 → 拿这套权重去"预测"这 1000 期里的每一期 → 统计命中率
```

这样算法其实"看到了未来"，回测结果没有意义。

**正确做法（滚动预测 / walk-forward）**：

```
用第 1~100 期  → 计算策略 → 预测第 101 期 → 与真实第101期开奖比较 → 记分
用第 1~101 期  → 计算策略 → 预测第 102 期 → 与真实第102期开奖比较 → 记分
用第 1~102 期  → 计算策略 → 预测第 103 期 → ……
```

即：`D[1:t] → 生成第 t+1 期候选号码 → 与真实第 t+1 期结果比较 → 累计成绩`。所有策略、包括未来要加的机器学习模型，都必须走这套流程，否则排行榜是假的。

### 3.4 防止"调参调出假优势"（多重比较 / 数据窥探问题）

如果你尝试了成百上千种参数组合，即使彩票完全随机，也几乎必然能"挑出"一个历史表现特别好的组合——这不是策略真的有效，而是选择性偏差。

对策：把数据严格分成三段，并且写进产品规则公开展示：

```
训练/开发集：用于设计和调整策略（例如前 60% 的历史期数）
验证集：用于筛选/调参（中间 20%）
最终盲测集：只用来做一次性最终评估，绝不能拿来反过来调参（最后 20%）
```

这一条建议直接写在网站的"方法论"页面里，作为你这个项目"讲科学"的证据，也是和一般"预测网站"最大的差异化。

### 3.5 比较"策略 vs 随机"要用配对差值检验，而不是分别看置信区间

与其分别看"策略的 CI 是否覆盖理论期望 1.0909"，不如在**同一期**上直接比较策略与随机基准的差值——因为每一期的真实开奖对所有策略都是同一个，天然构成配对数据：

```
D_i = H_策略,i − H_随机,i   （第 i 期，策略命中数 − 随机基准命中数）
```

对 {D_i} 做配对 t 检验（或更稳健的非参数符号秩检验），看均值是否显著偏离 0，比"各自的 CI 是否盖住 1.0909"更直接回答"这个策略到底有没有比随机强"。

**一个需要留意的细节**：滚动窗口的训练数据是逐期重叠的（第 101 期用第 1~100 期，第 102 期用第 1~101 期），有评审担心这会破坏样本独立性。严格来说，只要真实开奖本身逐期独立，命中数 D_i 之间的独立性并不会因为训练窗口重叠而被破坏（因为 D_i 只依赖于当期这一次独立开奖，跟策略是怎么算出来的无关）；但为了不过度依赖"真实开奖严格独立同分布"这个假设，**建议把 t 检验放在内部校验层，用户界面上优先展示 3.6 节的蒙特卡洛分位数**——它不需要假设正态分布，也更容易向非统计背景的用户解释。排行榜可以两者都存，但对外只讲分位数。

### 3.6 随机基准做成一条分布：固定真实开奖 + 生成大量独立随机策略

具体方法要说清楚，避免歧义——**不是打乱历史开奖顺序，而是固定住真实开奖序列不变，生成 5,000~10,000 条相互独立的"随机选号策略"，让它们都跑一遍同样的 Walk-Forward 流程**，得到一条经验分布：

```
random_run_0001 → 平均命中 1.088
random_run_0002 → 平均命中 1.102
random_run_0003 → 平均命中 1.075
...（5000~10000 条）
```

再把你真正要评估的策略（热号/综合/未来的 ML 模型）的得分插进这条分布里，直接读分位数。这样"综合策略跑赢了 73% 的随机策略"这句话就有了一个很好玩的潜台词——**它本质上只是恰好落在了随机分布的第 73 百分位**，这正是整个项目想讲的故事：听起来牛，其实就是运气分布里的普通一员。

这一节和 3.5 都不是要推翻 3.1~3.4 的期望值/置信区间框架，而是补一层"两两比较"的严谨性——长期均值+CI 给"绝对水平"的直觉，蒙特卡洛分位数给"是否真的更强"的结论。

### 3.7 不要只用"命中数"当唯一指标，分层展示

一个策略"好不好"不能只看平均命中数，建议从第一天就设计好分层指标，方便以后往上叠：

```
Level 1 基础命中：平均红球命中、蓝球命中率
Level 2 极端事件：≥4红/≥5红/6红 出现次数、一等奖组合次数
Level 3 命中分布：命中0~6个红球分别的次数分布（不是只看均值）
Level 4 资金层面：累计投入、累计奖金、ROI、最大回撤、期末余额
Level 5 统计层面：vs 理论期望、vs 随机蒙特卡洛分位数、显著性/证据等级
```

否则容易陷入"策略A平均1.095比策略B平均1.091好"这种误导性比较，而两者的命中分布可能完全不同（比如一个稳定在2左右，另一个偶尔中5个但大部分时候0个）。

---

## 四、系统架构

```
                    官方/公开历史开奖数据
                            ↓
                  ┌──────────────────┐
                  │   数据处理层      │  清洗 / 结构化 / 存成 JSON
                  └────────┬─────────┘
                            ↓
                  ┌──────────────────┐
                  │   策略/算法层     │  随机、热号、冷号(遗漏)、
                  │                  │  奇偶/分区/和值、加权综合、
                  │                  │  （后期可选）ML 模型
                  └────────┬─────────┘
                            ↓
                  ┌──────────────────┐
                  │  Walk-Forward     │  滚动预测 + 记分
                  │  回测引擎         │  统计均值/方差/置信区间
                  └────────┬─────────┘
                            ↓
                  ┌──────────────────┐
                  │   排行榜/统计层   │  收敛曲线、显著性检验、
                  │                  │  短期爆点(命中4+/5+次数)
                  └────────┬─────────┘
                            ↓
                  ┌──────────────────┐
                  │  AI 解释层(可选)  │  离线批处理生成"战报"，
                  │                  │  不参与号码生成决策
                  └────────┬─────────┘
                            ↓
                    静态网页（Cloudflare Pages）
                            ↑
                  GitHub Actions 定时任务
                  （开奖后自动抓数据→重算→提交→自动部署）
```

**部署形态：** 完全静态、零服务器常驻成本。

```
GitHub 仓库（数据 + 算法代码）
    ↓ 定时触发（开奖后）
GitHub Actions：抓取最新一期数据 → 跑回测 → 更新 JSON → （可选）调用一次 LLM 生成战报 → commit
    ↓ push 触发自动构建
Cloudflare Pages：静态资源 + 预生成 JSON，浏览器端 JS 直接计算"我的策略"结果
```

好处：前期完全不需要后端服务器/数据库；用户交互式生成（拖动权重滑块）在浏览器端用 JS 实时算，0 API 成本、0 延迟。

---

## 五、功能模块清单（严格按优先级开发）

1. **历史数据 + Walk-Forward 回测引擎**（地基，必须先做对）
2. **基础策略**：纯随机 / 热号 / 冷号(遗漏) / 奇偶+分区+和值综合 / 自定义加权
3. **统计展示**：收敛曲线、理论期望基准线、置信区间、显著性提示、排行榜
4. **短期"爆点"数据**：近10/30/100期表现、历史单期最高命中、命中4+/5+次数——这是内容传播的关键素材
5. **视觉与交互**（赛博/极客风格，开奖号码动画等）
6. **"我的策略"**：用户拖动权重滑块（冷热倾向/遗漏倾向/随机扰动等），前端实时生成号码。**这里要显式拆成两个数字**：一个是"历史回测成绩"（用户可以随便调权重、反复看这个数字，仅供娱乐，明确标注"不作为策略有效性证据"）；另一个是"盲测成绩"（用一段用户调参时完全看不到、无法拿来调整的历史区间跑出来的成绩，调参过程中始终隐藏，最后才展示）。否则用户会把"历史回测调到全站第一"当成"我发现了规律"，这其实只是数据窥探的一种用户端形式。
7. **AI 战报**：开奖后离线批处理生成一段"点评"，解释各策略本期表现，语气可以幽默、强调"没有显著跑赢随机"这一核心事实
8. **（后期）Agent Skill 接口**：例如 `/lottery 双色球` 自动拉取最新排行榜、按用户描述的偏好生成一组号码并附带统计学解释

**注意开发顺序**：先做 1→2→3，把回测引擎和统计学基础做对；4→5→6 是产品体验和传播点；7→8（AI 相关）放在最后，且第一版 AI 战报完全可以用模板文案代替，不调用任何模型。

**V2 补充模块**（来自后续评审，建议插入到对应阶段）：

- **长期资金模拟（对外正式名称，"韭菜模拟器"作为内部彩蛋模式名）**：给每个策略配一个虚拟账户，按真实奖级规则结算奖金，画出账户余额随开奖期数变化的曲线。奖级判定（命中几个红球+是否中蓝球 → 第几等奖）和实际奖金金额（一二等奖是浮动奖池，三等奖以下才是固定金额）要在数据结构里分开存，不要在展示层硬编码一个固定的"一等奖500万"，否则单期头奖会让曲线严重失真。默认参数建议做成可调（例如初始本金、每期投注注数），因为"1万元本金、每期1注"1000期下来只会缓慢下降，视觉冲击有限；调低本金或调高单期注数能更快看到"长期负期望"这条曲线，但具体倍率建议参考官方公开的奖级分布数据校准，而不是拍脑袋定，这条曲线的意义是科普"为什么长期负期望"，不是追求视觉刺激。
- **蓝球（特别号）必须建模**：双色球单中蓝球就有六等奖，不把蓝球和真实奖级规则接入回测，"资金曲线"和排行榜都是半成品，需要在阶段一的数据结构和回测引擎里就设计进去，而不是后补。
- **独立的"理论基准"层**：不要把"理论期望值 1.0909"混在"随机策略"这一项里，单独列一层 Benchmark，所有策略（含随机模拟、启发式规则、以后可能加的 ML 模型）都相对这条线比较，页面结构会更清楚。
- **版本号管理**：给数据集、算法规则各自加版本号，例如"数据集截至 2026-09-11 / Hot Strategy v1.1 / 回测区间第 X~Y 期"。因为算法规则以后一定会改（比如热号窗口从固定改成指数衰减），没有版本号的话，今天和两个月后的排行榜根本没法比较，也没法解释为什么某天排行榜数字突然变了。
- **自动生成传播素材**：GitHub Actions 每次重算后，顺手导出一张"本期擂台图/收敛曲线"静态图片，作为社媒/社区分享素材。这不是锦上添花，是这类数据/算法项目获取早期流量的现实手段，建议从阶段四就开始做。

---

## 六、数据结构设计（示例）

```
data/
 ├── ssq.json          # 双色球历史开奖
 ├── dlt.json          # 大乐透历史开奖
 ├── leaderboard.json  # 各策略累计回测成绩
 └── report_latest.json # 最新一期 AI/模板战报
```

单期开奖记录示例：

```json
{
  "period": "2026099",
  "date": "2026-09-10",
  "red": [5, 12, 17, 21, 28, 32],
  "blue": 7,
  "source": "gudaoxuri/lottery_history",
  "fetched_at": "2026-09-12T08:00:00Z"
}
```

`source` / `fetched_at` 用于多数据源交叉核对：如果两个数据源对同一期的开奖号码给出不一致的结果，正确做法是**暂停自动发布并报警，而不是随便挑一个继续跑**——抓错数据不被发现，比抓不到数据要恐怖得多。

策略打分中间结果示例（用于"为什么选中17号"这类可解释展示）：

```json
{
  "number": 17,
  "score": 0.67,
  "components": {
    "frequency": 0.71,
    "omission": 0.82,
    "recent_trend": 0.63,
    "zone_balance": 0.55,
    "random_noise": 0.19
  }
}
```

排行榜条目示例（加入版本号，避免规则改了以后排行榜无法对比）：

```json
{
  "strategy": "冷热综合",
  "strategy_version": "v1.1",
  "dataset_snapshot": "2026-09-11",
  "periods_tested": 1482,
  "mean_hit": 1.094,
  "theoretical_expectation": 1.0909,
  "ci_95": [1.072, 1.116],
  "vs_random_percentile": 63,
  "is_significant": false
}
```

单期回测结果示例（奖级判定和实际奖金分开存，一/二等奖是浮动奖金，三等奖以下是固定奖金）：

```json
{
  "period": "2026100",
  "predicted_red": ["05", "12", "17", "21", "28", "32"],
  "actual_red": ["05", "12", "19", "21", "28", "33"],
  "predicted_blue": "07",
  "actual_blue": "07",
  "red_hits": 4,
  "blue_hit": true,
  "prize_level": 5,
  "bonus_type": "fixed",
  "bonus": 10
}
```

---

## 七、关键算法示例（伪代码/JS）

**核心原则：两条评审都指出的问题必须先修——① 回测循环里"预测第几期"的索引不能错（off-by-one）；② 回测用的策略必须是确定性的或用固定种子的伪随机数，禁止裸用 `Math.random()`，否则同一份历史数据每次跑出的排行榜都会变，直接违背"同数据+同算法=同结果"的原则。**

约定两种模式：

```
交互模式（用户点"再来一组"）→ 允许普通 Math.random()，每次可以不同
回测模式（生成排行榜/收敛曲线）→ 必须用「期号 + 策略名」做种子的伪随机数，
                                 或者干脆用不含随机成分的确定性规则（如 Top-N）
```

### 7.1 种子伪随机数生成器（回测专用，保证可复现）

```javascript
// Mulberry32：轻量、够用的种子随机数生成器
function seededRandom(seed) {
  let a = seed;
  return function () {
    a |= 0; a = (a + 0x6D2B79F5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function hashSeed(str) {
  let h = 0;
  for (let i = 0; i < str.length; i++) h = (Math.imul(31, h) + str.charCodeAt(i)) | 0;
  return h;
}
// 用法：const rng = seededRandom(hashSeed(`${strategyName}-${period}`));
```

### 7.2 多指标归一化后再加权（避免不同量纲直接相加）

```javascript
// 先归一化到 0~1，再加权，不要把频次和遗漏期数直接相加
function normalize(valuesMap) {
  const values = Object.values(valuesMap);
  const min = Math.min(...values), max = Math.max(...values);
  const range = max - min || 1;
  const result = {};
  for (const [k, v] of Object.entries(valuesMap)) {
    result[k] = (v - min) / range;
  }
  return result;
}
```

### 7.3 确定性基准策略（回测用，例如"热号 Top-6"，无随机成分）

```javascript
// 严格确定性：按分值降序排列，分值相同按球号升序打破平局
function hotStrategy(trainData, windowSize = 50) {
  const windowData = trainData.slice(-windowSize);
  const freqMap = {};
  for (let i = 1; i <= 33; i++) freqMap[String(i).padStart(2, "0")] = 0;

  windowData.forEach((draw) => {
    draw.red.forEach((ball) => { freqMap[ball] = (freqMap[ball] || 0) + 1; });
  });

  const sortedBalls = Object.keys(freqMap).sort((a, b) => {
    if (freqMap[b] !== freqMap[a]) return freqMap[b] - freqMap[a];
    return Number(a) - Number(b); // 平局按球号升序，保证结果唯一确定
  });

  const blueFreq = {};
  windowData.forEach((draw) => { blueFreq[draw.blue] = (blueFreq[draw.blue] || 0) + 1; });
  const bestBlue = Object.keys(blueFreq).sort((a, b) => blueFreq[b] - blueFreq[a])[0] || "01";

  return {
    red: sortedBalls.slice(0, 6).sort((a, b) => Number(a) - Number(b)),
    blue: bestBlue,
  };
}
```

### 7.4 带权重的策略（交互模式用普通随机，回测模式传入种子 rng）

```javascript
// rngFn 由调用方传入：交互模式传 Math.random，回测模式传 seededRandom(...)
function generateRedBalls(weightsMap, rngFn = Math.random) {
  const balls = Array.from({ length: 33 }, (_, i) => String(i + 1).padStart(2, "0"));
  const selected = [];
  const w = { ...weightsMap };

  for (let i = 0; i < 6; i++) {
    const total = balls.reduce((sum, b) => sum + (w[b] || 1), 0);
    let r = rngFn() * total;
    for (const ball of balls) {
      r -= w[ball] || 1;
      if (r <= 0) {
        selected.push(ball);
        balls.splice(balls.indexOf(ball), 1);
        break;
      }
    }
  }
  return selected.sort((a, b) => Number(a) - Number(b));
}
```

### 7.5 Walk-Forward 回测主循环（已修正 off-by-one，并加入数据顺序防御）

```javascript
/**
 * history.slice(0, t) 取到的是下标 0 ~ t-1，即"前 t 期"。
 * 那么第一个还没被看过的期就是下标 t 本身，
 * 所以待预测目标必须是 history[t]，而不是 history[t+1]
 * （原方案这里写成了 t+1，白白跳过了一期，必须修正）。
 */
function walkForwardBacktest(rawHistory, strategyPredictFn, minTrainSize = 100) {
  // 【关键防御】国内大多数公开彩票数据源/API返回的是"最新一期在前"的倒序列表。
  // 如果不做这一步，slice(0, t) 拿到的其实是"未来"的数据去预测"过去"的期数，
  // 是比 off-by-one 更严重的数据穿越问题，必须在入口处强制排序。
  const history = [...rawHistory].sort((a, b) => Number(a.period) - Number(b.period));

  const records = [];
  for (let t = minTrainSize; t < history.length; t++) {
    const trainData = history.slice(0, t);   // 只能看到前 t 期
    const actual = history[t];               // 真实第 t+1 期（0-indexed 下标 t）

    const predicted = strategyPredictFn(trainData, actual.period); // 可用 actual.period 作种子

    const redHits = actual.red.filter((b) => predicted.red.includes(b)).length;
    const blueHit = actual.blue === predicted.blue;

    records.push({
      period: actual.period,
      redHits,
      blueHit,
      prize: calculatePrize(redHits, blueHit),
    });
  }
  return records;
}

// 双色球奖级判定：只判定"中了第几等奖"，不在这里决定具体金额
function calculatePrize(redHits, blueHit) {
  if (redHits === 6 && blueHit) return { level: 1, bonusType: "floating" }; // 一等奖，实际金额取决于当期奖池
  if (redHits === 6) return { level: 2, bonusType: "floating" };            // 二等奖，同样浮动
  if (redHits === 5 && blueHit) return { level: 3, bonusType: "fixed", bonus: 3000 };
  if (redHits === 5 || (redHits === 4 && blueHit)) return { level: 4, bonusType: "fixed", bonus: 200 };
  if (redHits === 4 || (redHits === 3 && blueHit)) return { level: 5, bonusType: "fixed", bonus: 10 };
  if (blueHit) return { level: 6, bonusType: "fixed", bonus: 5 };
  return { level: 0, bonusType: "fixed", bonus: 0 };
}

// 浮动奖金（一、二等奖）用官方公布的历史平均值做近似估算，不需要还原奖池分配算法
function estimateFloatingBonus(level, historicalAverages) {
  return historicalAverages[level] ?? 0; // historicalAverages 可用近几年官方公布的一/二等奖平均单注金额
}

function summarize(records) {
  const n = records.length;
  const mean = records.reduce((s, r) => s + r.redHits, 0) / n;
  const variance = records.reduce((s, r) => s + (r.redHits - mean) ** 2, 0) / (n - 1);
  const stderr = Math.sqrt(variance / n);
  return { meanHit: mean, ci95: [mean - 1.96 * stderr, mean + 1.96 * stderr] };
}

// 配对差值检验：strategyRecords 和 randomRecords 必须是同一段历史、逐期对齐
function pairedDiffTest(strategyRecords, randomRecords) {
  const diffs = strategyRecords.map((r, i) => r.redHits - randomRecords[i].redHits);
  const n = diffs.length;
  const meanDiff = diffs.reduce((a, b) => a + b, 0) / n;
  const variance = diffs.reduce((s, d) => s + (d - meanDiff) ** 2, 0) / (n - 1);
  const stderr = Math.sqrt(variance / n);
  const tStat = meanDiff / stderr; // 可再查 t 分布表或用近似正态得到 p 值
  return { meanDiff, tStat };
}
```

> 一、二等奖是浮动奖金，实际由当期奖池和中奖注数决定，不需要也不建议还原真实奖池分配算法，用官方历史公布的平均单注金额估算即可（见 `estimateFloatingBonus`）；三等奖以下是固定金额，直接写死没问题。这个拆分也是为了避免"资金曲线"因为个别期头奖数字失真——用近似值远比自己编一个固定的"500万"靠谱。

---

## 八、AI 的角色定位

**原则：AI 不决定号码，只解释已经算好的确定性结果。**

流程：

```
算法层输出结构化数据（各策略评分、命中统计、排行榜变化）
        ↓
（开奖后，离线批处理，一次性调用，非实时）
AI 读取这些结构化数据，生成一段自然语言"战报"
        ↓
静态网页展示战报
```

Prompt 设计要点：

- System 层面要求：语气可以幽默/略带调侃，但必须客观、基于给出的真实数字，不允许编造未提供的统计结论。
- 每次调用都必须明确告知"本期是否有策略显著跑赢随机"这一核心事实，避免暗示某策略"更准"。
- 输出长度限制在 150~250 字，避免变成一本正经的伪科学分析。

第一版建议：**完全不接 AI API，用模板文案代替战报**（例如根据数字自动填空生成句子），先把回测引擎和数据管道跑稳，再逐步引入 LLM，降低成本和不确定性。

---

## 九、开发路线：分阶段控制范围，第一版只做双色球

两条评审都指出了同一个风险：**方案讨论到这个阶段，很容易从"7天能跑起来的小项目"膨胀成"小型统计研究平台"**。对策是明确分阶段，并且**V0/V1 阶段只做双色球一种彩票**，把架构抽象成可复用的 `LotteryGame` 之后，再接大乐透等其他玩法——不要一开始就双色球、大乐透一起做，那样项目会变成"彩票数据维护工程"而不是"概率实验室"。

**V0（先验证核心结论，越丑越好，目标是几天内跑出真实数字）：**
- 双色球历史数据（哪怕只有近 500~1000 期）+ 蓝球 + 奖级规则，从一开始就设计进数据结构。
- 独立的"理论基准"层（Benchmark，1.0909）。
- 三个策略：随机、热号、冷号，且都是确定性或种子随机（参考 7.1/7.3 节代码）。
- Walk-Forward 回测引擎，注意 7.5 节的两个防御点：off-by-one 索引 + 按期号升序排序。
- 输出：平均命中数 + 收敛曲线。

跑到这一步就该先停下来问自己："这三个策略在真实历史数据上到底是什么成绩？"——只要这个数字出来了，后面的一切都是基于真实数据继续做，而不是脑补设计。

**V0.5（把统计严谨性叠上去）：**
- 配对差值检验 + 蒙特卡洛随机分布分位数（3.5/3.6 节）。
- 命中分布展示（不只是均值）。
- 排行榜、短期爆点数据（近10/30/100期表现）。

**V1（做成能玩的产品）：**
- 视觉与交互设计（赛博/极客风格、开奖动画）。
- "我的策略"权重滑块，历史回测成绩 + 盲测成绩分开展示（见第五节第 6 条）。
- 蓝球和奖级规则接入回测，做出长期资金模拟（"韭菜模拟器"作为彩蛋模式）。

**V1.5（自动化管道）：**
- GitHub Actions 定时抓取数据 → 重跑回测 → 更新 JSON → 提交；接入 Cloudflare Pages 自动部署。
- 抓取脚本必须做重试 + 多数据源容灾，并且严格核对多个数据源是否一致，不一致就停止自动发布并报警，而不是随便挑一个继续跑。
- **关于数据源**：网上能找到一些开源的历史开奖数据镜像仓库（例如 GitHub 上有维护"双色球/大乐透历史数据"JSON 文件的开源项目），可以作为起点，但字段结构、更新频率、准确性都需要自己核实，不要盲目相信任何一个来源（包括网上流传的现成脚本里写的 API 地址——有些看起来像官方接口，实际上是不存在或已失效的占位符，运行前务必自己验证能不能连通、返回的数据对不对）。
- 顺手导出一张"本期擂台图/收敛曲线"静态图片，作为传播素材。

**V2（AI 和更多玩法，最后再做）：**
- AI 战报（先模板文案，验证没问题后再接一次性离线 LLM 调用）。
- 机器学习模型（XGBoost/LSTM/Transformer 等）——不用急，等前面的基线策略都跑完、结论稳定以后再加，避免"为了 AI 而 AI"。
- 第二种彩票（大乐透等），复用 `LotteryGame` 抽象。
- Agent Skill 接口（`/lottery` 类查询）。

**Agent Skill 作为后续迭代**：待网页有真实数据积累和访问反馈后，再开发 `/lottery` 类 Skill，让用户可以用自然语言查询排行榜、生成个性化权重策略并附带统计解释。

---

## 十、内容与传播角度

这个项目最大的传播点不是"预测准不准"，而是"用真实数据把玄学打回原形"的过程本身。可用素材：

- "回测 1000+ 期：机器学习模型的命中率和随机数几乎一模一样" —— 收敛曲线截图。
- "算法打脸排行榜"：定期公布哪个策略本期"翻车"、哪个"暴击"，强调这只是短期波动。
- "方法论"页面公开 Walk-Forward 回测规则和训练/验证/盲测分割方式，作为"这不是玄学网站"的证据，适合在技术社区（V2EX、知乎技术区等）分享。

---

## 十一、商业化边界

**可以做的：**
- 展示广告（非彩票相关）。
- 高级统计功能（更长历史回测、自定义策略保存、多期对比）作为增值点。
- 内容衍生（数据可视化文章、方法论解读）积累流量。

**绝对不能做的：**
- 不能卖"预测号码"、VIP 精准版、专家推荐。
- 不能接入购彩、代购、合买、充值、返利等任何交易环节。
- 不能使用"保底""稳赚""内部消息"等误导性话术。
- 不能在生成的号码、资金曲线旁放"立即购买""一键下单"之类的交互按钮，即便只是链接到第三方购彩平台也不行。

---

## 十二、风险与对策清单

| 风险 | 对策 |
|---|---|
| 被误解为"教人买彩票"的灰产网站 | 首页/方法论页明确声明实验性质，不接入任何购彩环节 |
| 回测存在数据泄漏，结论不可信 | 严格执行 Walk-Forward 滚动回测，公开回测规则 |
| 调参调出"虚假优势"（数据窥探） | 训练/验证/盲测三段式分割，盲测集不参与调参 |
| 实时调用 LLM 成本高、延迟大、结果不可复现 | AI 只做开奖后离线批处理战报，号码生成由确定性算法完成 |
| 短期波动被误读为"策略有效" | 同时展示长期均值、置信区间、显著性判断，避免只放一个亮眼数字 |
| 域名/内容被误判为违规 | 避免"预测""稳赚"等敏感词，突出"实验室/统计科普"定位 |
| 数据源反爬/云端 IP 被限流封禁 | 优先用开放 API、开源镜像数据源；抓取脚本加重试 + 多数据源容灾，不要死磕官网反爬 |
| 文案过度宣称"已证明随机"，被挑理 | 统一表述为"模型假设下期望相同，且当前样本无显著背离证据"，避免绝对化断言 |
| 数据源返回"最新在前"的倒序列表，直接喂进回测会用未来数据预测过去 | 回测引擎入口处强制按期号升序排序，作为默认防御（见 7.5 节代码） |
| 用户在"我的策略"里反复调权重，把历史回测调到第一名当成"发现规律" | 历史回测成绩和盲测成绩分开展示，明确标注历史回测"仅供娱乐，不作为有效性证据" |
| 网上流传的抓取脚本里 API 地址是占位符或已失效 | 使用前先手动验证能否连通、返回字段是否符合预期，不要直接信任未验证的第三方代码 |

---

## 十三、技术栈建议

```
前端：原生 JS 或轻量框架（无需复杂框架），ECharts 做收敛曲线/排行榜可视化
数据：JSON 静态文件（历史开奖、回测结果）
算法：TypeScript/JavaScript 实现，全部跑在浏览器端或 GitHub Actions 构建时
自动化：GitHub Actions（定时抓取数据 + 重跑回测 + 提交）
部署：Cloudflare Pages（静态托管，免服务器）
AI（后置、可选）：任一 LLM API，仅用于开奖后离线批处理生成"战报"文案
```

第一版可以做到**纯 HTML + CSS + JS + 静态 JSON**，不需要任何数据库或常驻后端。

---

## 十四、开发优先级总结

1. 历史数据 + Walk-Forward 回测引擎（最优先，决定项目是否"讲科学"）
2. 基础策略（随机/热号/冷号/综合）
3. 收敛曲线 + 理论期望线 + 置信区间 + 排行榜
4. 视觉与交互设计
5. "我的策略"参数调节
6. AI 战报（先模板，后 LLM）
7. Agent Skill 接口（最后接入）

**核心提醒：不要从 AI 开始做。先把"能证明策略是否真的跑赢随机"的回测引擎做对，这是整个项目唯一不能返工的部分；UI、数据源、AI 都可以后续替换或升级。**

---

## 十五、V2 修订记录（基于多方评审）

- 修正 7.5 节 Walk-Forward 回测代码的 off-by-one 索引错误：待预测目标是 `history[t]` 而非 `history[t+1]`。
- 回测中的策略生成不再允许裸用 `Math.random()`：确定性策略用固定排序规则，需要随机成分的策略在回测时必须用「期号+策略名」做种子的伪随机数，保证同一份数据永远得到同一份排行榜。
- 新增 3.5/3.6 节：用配对差值检验和随机模拟分布来比较"策略 vs 随机"，比分别看置信区间更直接、更有说服力。
- 新增蓝球和真实奖级规则、资金归零曲线，作为比"平均命中数"更直观的科普素材。
- 收紧 3.1 节措辞，避免"已被数学证明随机"这类绝对化表述。
- 风险表新增：数据抓取容灾（防云端 IP 被限流）、文案措辞风险。

---

## 十六、V3 修订记录（第三轮评审）

- 新增项目北极星原则："任何策略在没有通过盲测之前，都不准自称有效"，写进方法论页面。
- 明确禁止在生成号码/资金曲线旁放置任何"购买/下单"类按钮或第三方购彩链接。
- 3.5 节补充配对 t 检验的独立性假设讨论，建议用户界面优先展示 3.6 节的蒙特卡洛分位数而非 p 值。
- 3.6 节把"随机基准分布"的方法讲清楚：固定真实开奖序列 + 生成大量独立随机策略，而不是打乱开奖顺序。
- 新增 3.7 节：分层 KPI 体系（基础命中/极端事件/命中分布/资金/统计），避免只用平均命中数做比较。
- "我的策略"拆分为"历史回测成绩"和"盲测成绩"两个数字，防止用户在界面上通过反复调参制造数据窥探。
- 奖级判定与实际奖金金额分离（`prize_level` + `bonus_type: floating/fixed`），避免硬编码固定头奖金额导致资金曲线失真。
- 数据结构加入 `source`/`fetched_at`/版本号字段，用于多数据源交叉核对和排行榜可比性。
- 7.5 节回测引擎入口加入按期号强制升序排序的防御代码，防止数据源"最新在前"的倒序列表导致用未来数据预测过去。
- 第九节重写为 V0→V0.5→V1→V1.5→V2 分阶段路线，第一版明确只做双色球一种彩票，避免范围膨胀成"彩票数据维护工程"。
- 风险表新增：数据倒序陷阱、用户端数据窥探、未验证的第三方脚本/API 地址。

---

## 十七、V4 方案：把"预测"做成受控功能，同时把安全底线钉成代码

> 本节是 V3 之后的新增章节。V3 之前所有版本都在回答"策略是不是真的比随机强"；V4 要回答一个更危险、但迟早要面对的问题：**用户是冲着"预测"来的，要不要给、给到什么程度、给的时候怎么保证产品不滑向售彩导流。**
>
> 结论先写在最前面：**"预测"可以做，但只能做成"被检验的对象"，不能做成"被提供的结论"**；并且——**在没有先把安全底线落成代码之前，不发布任何预测相关界面。**

### 17.1 先承认一个产品事实：用户要的是预测，不是排行榜

前面 V0~V1 的所有功能（Walk-Forward、置信区间、蒙特卡洛分位数、盲测集）都是"科学"的一半，但它们回答的是"哪个策略更强"，而不是用户真正在搜的那句话："**下一期买什么号。**"

只要这句话没有被正面回应，产品就会陷入一个尴尬处境：

- 用户找不到他要的东西 → 去别处找 → 找到了那些真的在卖"预测号码"的站；
- 而本项目越是回避这个话题，越显得"你这网站什么用都没有"，而不是"你这网站很诚实"。

所以 V4 必须正面回应它。但回应方式决定了这个项目是"概率实验室"还是"预测网站"——差别不在于**有没有那个功能**，而在于**那个功能输出的东西，叫不叫"预测结果"**。

一句话原则（建议同时写进方法论页面）：

> **本实验室把"预测"当作一个需要被检验的假设来展示，而不是当作一个可以交付的服务来提供。**
> 展示"某一组号码的生成规则"，并同时展示"它凭什么不成立"。

### 17.2 先明确否掉三个方案（省得以后走回头路）

| 候选方案 | 为什么否掉 |
|---|---|
| **A. 直接给"下一期推荐号码"** | 这是本项目的立项红线（1.1/1.3 节）。一旦输出"推荐"，无论加多少免责声明，产品性质都会从"实验"变成"预测服务"，且与官方"AI 预测彩票号码没有科学依据"的提示正面冲突。**永久否决，不做任何变体。** |
| **B. 用 LLM/ML 输出号码并包装成"AI 研判"** | 属于 A 的变体，且更糟：模型在随机序列上并没有可利用结构（3.1 节），输出只会是随机数 + 一层让用户误以为有依据的话术。这正好是项目要打脸的对象，自己去做就变成了自我否定。 |
| **C. 用户填生辰八字/梦境/直觉，系统"算出"号码** | 这是玄学产品的标准形态，与项目"打脸玄学"的定位直接相反；而且它制造的是**用户对系统的信任**，而不是**用户对随机性的理解**，风险收益完全反向。 |

**那做什么？**做 17.3 的四层功能：每一层都输出"数字 + 这个数字能不能被当作依据"。用户能得到他要的"下一期号码"这个东西，但拿到的是**一组带完整统计诊断的样本**，而不是一个"推荐"。

### 17.3 V4 核心功能设计：受控"预测"（对外命名建议：隔空打靶 / 下一期观测）

> 命名注意：**不要**用"预测中心""AI 预测""智能选号"这类词做栏目名。建议用**"隔空打靶"**（实验语气）或**"下一期观测"**（观测语气）。栏目名本身就是第一道防线。

#### 第 1 层：数字体检报告（零风险，优先做）

对**每一个号码**给出它在历史数据里的表现，并明确标注这个数字的性质：

```
号码 17
├── 全历史出现次数：48 次 / 251 期
├── 理论期望次数：251 × 6/33 ≈ 45.64 次
├── 95% 置信区间：[35.6, 58.5]  ← 二项分布，直接算，不需要假设
├── 本号码是否偏离理论期望：否（落在区间内）
├── 最近 50 期出现：11 次（理论期望 9.09 次，区间 [4.4, 15.9]）
└── 结论：该号码的出现频率与"每期等概率"完全一致，未表现出任何可利用的倾向
```

这一层的价值在于：**它把用户"想看的数字"全都给了，但每一个数字后面都站着一条置信区间。** 用户想看的"冷热"，这里全都有，而且比任何预测网站给得更细——只是它同时告诉用户"这些差异都在噪声范围内"。

#### 第 2 层：遗漏期数的几何分布参考（正面回答"该出了吧"）

这是用户提问频率最高的一句话，必须有正面答案：

```
号码 09 当前遗漏 27 期
├── 在"每期等概率"假设下，单个号码遗漏 k 期的概率：P(遗漏 ≥ k) = (1 − 6/33)^k = (27/33)^k
├── P(遗漏 ≥ 27) = (27/33)^27 ≈ 0.00283，即平均每 353 个"号码-期"里就会出现一次
├── 33 个红球 × 251 期 ≈ 8283 个"号码-期"，按此概率预期出现约 23 次这种长度
├── 结论：27 期遗漏在这份数据里属于完全正常的波动，不是"该出了"的信号
└── 反面提示：如果这份数据里从未出现超过 X 期的遗漏，那才是值得写进报告的反常现象
```

这一段建议做成页面上的**交互小工具**：点任意号码 → 显示它当前遗漏 + 这个遗漏长度的理论稀有度 + 历史同期同长度的实际发生次数。**用一次点击把"该出了"这个念头拆掉**，效果比任何一段文字说明都强。

#### 第 3 层：形态诊断（把"选号玄学"逐条量化）

用户选号时真正纠结的是形态，不是单个数字。这一层对**任意一组号码**（无论是他手输的、还是本页生成的）做诊断：

| 检查项 | 诊断输出 | 想传达的事实 |
|---|---|---|
| 奇偶比 | 4:2，历史占比 24.4% | 各种奇偶比的出现比例由组合数决定，不是"平衡才好" |
| 大小比（1-16 / 17-33） | 3:3，历史占比 33.1% | 同上 |
| 三区分布（1-11 / 12-22 / 23-33） | 2:2:2，历史占比约 12.2% | "均匀分布"其实并不更常见 |
| 和值 | 96，历史均值约 102，区间 [45, 159] | 和值集中在中间是组合爆炸的结果，不是规律 |
| 连号 | 有 1 组（17,18），历史占比约 52% | 连号一点都不稀奇 |
| 与历史开奖重复 | 未与任何一期完全相同 | 但这不提高中奖率 |
| 历史同形态出现率 | 该形态组合数 / 全部组合数 = X% | **最关键的一列**：形态"稀有"≠中奖概率高 |

最后一行是这一层的灵魂：**任何形态限制都只是把可选组合从 1107 万减少到更少，它改变的是"你选了哪些组合"，而不是"这些组合会不会被开出来"。**建议给一个"形态过滤器"的交互演示——让用户加上 5 个"看起来更专业"的过滤条件，页面实时显示"可选组合从 11,075,568 组降到 74,613 组"，然后紧跟一句："中奖概率并没有提高，你只是把同样的 2 元押在了更少、更集中的组合上。"

#### 第 4 层：下一期观测（"预测"按钮的受控形态）+ 三重对照

这是用户真正想要的那个按钮，也是**唯一需要严格约束的界面**。设计规则：

**① 一次给 3 组参数化候选，不给 1 组"最优"**

```
观测组 A：热号规则（近50期频率 Top6 + 蓝球频率 Top1）
观测组 B：遗漏规则（遗漏期数 Top6 + 蓝球遗漏 Top1）
观测组 C：你设定的权重规则（见"我的策略"）
随机对照组 R：纯随机生成（每次点击重新抽，不参与任何"比较"叙事）
```

**② 每一组旁边必须并排三样东西，缺一不可**

```
├── 开发集历史成绩（该规则在过去 X 期上的平均命中）
├── vs 随机分布分位数（%）
├── 证据等级：无证据 / 样本量不足 / 弱证据 / 中等证据（自动标注，见 17.5 L3）
```

**③ 一句话结论由系统自动生成，且必须包含"与随机不可区分"**

> "以上 3 组观测组 + 1 组纯随机对照组，在历史数据上的成绩分别为 1.09 / 1.11 / 1.10 / 1.09。四个数字之间的差异全部落在随机波动范围内（全部证据等级：样本量不足）。**这 4 组号码在统计上没有任何一组比其它三组更可能中奖。**

**④ 硬性禁止清单（写进代码，见 17.5 L1 的自动审计）**

- 禁止出现"推荐""最优""本期看好""稳""必出""命中率提升"等词；
- 禁止把任何一组标成"主推""首选"或加视觉高亮；
- 禁止按"预测概率"给号码排序或给出概率数值；
- 禁止在观测组/资金曲线附近出现任何"购买/下单/复制去投注"按钮或第三方购彩链接（1.3 节红线）；
- 禁止记录并展示"上期预测命中了几个"这种**事后正确性叙事**——这是预测网站最经典的成瘾机制。可以展示"上期各组表现对照"，但必须同时展示随机对照组，且标题必须是"上期各组表现（含随机对照组）"而不是"上期命中回顾"。

> 关于第 5 条特别说明：**"上期我们预测中了 3 个红球"是本项目最危险的一句话**，即使它 100% 属实。因为随机对照组几乎每期也能中 2~3 个，抽掉对照组单独展示等于制造了一个假因果。要么两组一起上，要么一个都别上。

#### 第 5 层：预测悖论演示（建议作为 V4 的传播主素材）

这是 V4 最有价值的一块，也是本项目相对其它所有"预测网站"的**唯一不可复制的内容**：

**演示 A —— 就算你真有 1% 的优势，期望仍然是负的。**

```
假设某策略能把红球命中期望从 1.0909 提高到 1.10（约为 0.8% 的相对优势，已经比本项目所有策略都强）
→ 该优势对奖金期望的改善：约 0.8% × (可影响奖级部分的期望)
→ 双色球单注期望回报（按固定奖级 + 浮动奖级近似值估算）：约 0.9~1.0 元 / 每注 2 元
→ 结论：把优势放大到 10%，期望回报依然远低于成本
→ 要让期望回本，需要的优势量级是"百倍"而不是"百分之一"
```

**演示 B —— 命中概率表（用组合数精确计算，不引用外部数字）**

```
一等奖  6红+蓝     C(6,6)×1 / C(33,6)×16 = 1 / 17,721,088
二等奖  6红        15 / (C(33,6)×16)
三等奖  5红+蓝     6×C(27,1) / (C(33,6)×16) ...
……
六等奖  仅蓝球     1 / 16
合计中奖概率 ≈ 6.7%，其余约 93.3% 为 0 元
单注期望：约 0.94 元 / 成本 2 元（浮动奖级按近似均值估算）
```

**演示 C —— "覆盖更多组合"的错觉。** 买 10 注把一等奖概率从 1/1772 万提高到 1/177 万——提高是真的，但仍然约等于 0。这个演示用来回应"那我多买几注/加倍投注是不是就行了"。

**这三块必须由代码从组合数直接算出**，不允许在页面上硬编码任何"经验数字"。原因很简单：一旦这些数字是手写的，它们就变成了可被质疑的"说法"；由 `C(33,6)` 算出来的，是**可被任何人复算的事实**。这也是本项目区别于所有话术型网站的地方。

### 17.4 V4 功能清单与范围控制

**做：**

1. **安全底线三件套（第 0 步，先于一切功能）**：`scripts/audit-safety.js`、`scripts/verify-data.js`、构建流程强制门禁（详见 17.5）。
2. **数字体检报告**（17.3 第 1 层）——纯历史统计 + 二项置信区间。
3. **遗漏几何分布工具**（17.3 第 2 层）——交互，回答"该出了吧"。
4. **形态诊断器**（17.3 第 3 层）——含"形态过滤器让组合数变少但概率不变"演示。
5. **命中概率表 + 预测悖论演示**（17.3 第 5 层）——全部由组合数精确计算。
6. **下一期观测（受控预测界面）**（17.3 第 4 层）——3 组规则 + 1 组随机对照，三种指标并排，证据等级自动标注。
7. **证据等级自动标注**（17.5 L3）——把"能不能算有效"从一句人工写的免责声明，变成数据驱动的自动结论。
8. **资金模拟器参数化**（方案第五节 V2 补充模块遗留项）——初始本金/每期注数可调，让"长期负期望"从口号变成用户自己拉出来的曲线。

**明确不做（写下来就是为了以后不自作主张地加）：**

- 不做单一"推荐号码"，不做号码概率排序，不做"AI 研判本期"；
- 不接收任何用户输入的"个人化信息"（生日、姓名、梦境等）来生成号码——这会把产品从统计工具变成玄学工具；
- 不接大乐透等第二种彩票（优先级排在做完 V4 统计功能之后，见 17.6）；
- 不接 Agent Skill / 真实 LLM 战报（等 V4 统计层稳定后再谈，理由同 V2 原则：AI 只解释已算好的确定性结果）。

### 17.5 【本节是全篇最重要的一节】安全底线：四层防线，逐层落成代码

前面 1.3 / 十一节的"绝对不能做"目前**只是文档里的一段话**。文档约束不了未来自己或任何接手者的手，也会在一次次"就加一个小功能"中慢慢被磨掉。V4 的核心交付物之一，就是把这四层防线**变成会让构建失败的可执行检查**。

**总原则：底线不能靠自觉，要靠跑不过去的构建。**

#### L1 合规红线层（硬性、可自动审计、碰了就不许发布）

管线：`scripts/audit-safety.js <generated.html> [src]`，在 `build-site.js` 之后运行，**发现违规直接以非零码退出，页面视为未产出**。

| 审计项 | 检测方式 | 处理 |
|---|---|---|
| 违禁话术 | 违规词表（售彩类/支付类/承诺类/权威类，见下） | 命中即 fail |
| 交易类交互 | `<button>`/`<a>` 文本含"购买/投注/代购/充值/合买/加倍"（"购买"仅在明确表述为数学概念时白名单） | 命中即 fail |
| 外链 | 页面所有 `href` 必须为空锚点、站内锚点或明确列入白名单的文档链接 | 命中即 fail |
| 表单 | 页面不得存在任何 `<form>` / 输入提交控件 | 命中即 fail |
| 免责声明 | 页面必须同时包含"不提供售彩/代购/充值/返利"与"独立随机事件"两段声明 | 缺失即 fail |
| 合规审计清单 | 详情页/方法论页必须列出本审计的通过项与运行时间 | 缺失即 fail |
| "预测"一词 | 允许出现（V4 起明确允许），但**必须逐处检查上下文**：出现在"预测不是事实/预测需要被检验/预测不构成建议"等否定或方法论语境中放行；出现在号码结果旁、承诺语境、或与"准/准不准"搭配时 fail | 命中即 fail |

违规词表初版（分组，便于以后扩充）：

```
售彩类：购买 投注 下注 代购 合买 充值 返利 提现 购彩 站 稳赚 保底
支付类：微信 支付宝 二维码 打赏 付费 会员 VIP 内部号
承诺类：必中 必出 稳中 包中 高命中率 精准 命中率提升 恭喜中奖 帮你中
权威类：专家推荐 内部消息 内幕 独家 官方合作 授权
```

> 关于"预测"：这是 V4 唯一放宽的词，但**放宽的是"方法论叙事"，不是"号码结论"**。所有使用点会被逐一列进审计报告，方便事后复查。

#### L2 数据层防线（脏数据不许进管线）

管线：`scripts/verify-data.js`，在 `prepare-data.js` 之后、`build.js` 之前运行，**任一硬校验失败即停线**。

硬校验（失败则 fail，不允许"警告后继续"）：

- 期号唯一、严格递增；日期严格递增且落在合理区间；
- 红球恰好 6 个、互不相同、均在 1~33；蓝球在 1~16；均为两位字符串格式；
- 每期红球升序存储；`source` / `fetched_at` 字段齐全（缺失即 fail，因为这是多源交叉核对的前提）；
- 奖级函数 `calculatePrize` 全枚举自检：把 (0..6 红命中) × (蓝中/不中) 共 14 种取值全部跑一遍，逐条断言结果符合官方奖级规则表——**这一条能独立证明"奖级判定"这件事算对了**，比任何人工检查都可靠；
- 组合层自检：所有奖级概率之和 ≤ 1，中奖概率合计与"总组合数 − 不中奖组合数 / 总组合数"精确一致。

软校验（输出报告，不阻断，但会显著标注在页面）：

- **分布自洽性**：红球出现频次的卡方检验、蓝球频次卡方检验、和值分布、奇偶/大小比分布是否与理论一致；任一检验显著（p<0.01）都要在页面上明示"本数据集出现偏离，可能是数据错误，也可能是随机波动，已标注待复查"；
- **与官方核对抽样**：输出"最早 5 期 + 最近 5 期"的完整号码 + 期号 + 日期清单，作为**人工核对用的抽查表**（README 与页面同时展示）。任何人都能拿这张表去官方渠道逐条核对——这是目前唯一能替代"多数据源自动交叉核对"的诚实做法；
- **数据指纹**：对 `ssq.json` 内容计算 SHA-256 并写入 `report.json`，页面展示指纹前 12 位。这样"这份排行榜是拿哪份数据算的"变成可验证的，而不是靠信任。

> **关于当前唯一数据源的最新结论（必须如实写进 README 与页面）**：`gudaoxuri/lottery_history` 至今未能核实真实存在，**这是 V4 仍未解决、且优先级高于所有预测功能的遗留风险**。L2 防线的作用不是"解决"它，而是**把它从"隐藏的信任假设"变成"页面上公开可见的已知限制"**：数据自洽性校验只能证明"这份数据内部没有自相矛盾"，**不能证明它来自官方**。在换成可核实数据源之前，页面必须挂着这条说明，并且**不接自动化管道**（数据不可信时，自动化只会更快地传播错误）。

#### L3 统计层防线（防止"假有效"结论被展示出来）

这些规则**不再是界面文案的自觉，而是由 `build.js` 依据数据自动计算的结论字段**：

| 规则 | 自动行为 |
|---|---|
| 样本量下限 | 任一数据段期数 < 100 时，该段所有结论强制标注"样本量不足"，且**不得**标注任何显著性等级 |
| 置信区间跨期望 | 95% CI 覆盖理论期望 → 标注"与理论期望无显著差异" |
| 多重比较校正 | 本项目同时检验的策略数 N（含将来的 ML 模型）会随版本增加；对所有策略的配对检验 p 值统一做 Benjamini–Hochberg FDR 校正，用校正后的结果作为"是否显著"的唯一依据（`is_significant` 字段口径随之改为校正后） |
| 分位数幻觉 | `vs_random_percentile` 展示时必须同时显示"这是与 5000 条随机策略比较的位次"，禁止单独展示"跑赢 73% 的策略"这种句式 |
| 开发集/盲测集不可混用 | 盲测区间自封存日起只增不减（沿用 V0.5 的固定期号边界），且盲测集的任何数字不得出现在"我的策略"调参界面的可见区域 |
| 证据等级（对外唯一结论字段） | 由数据自动推导：`无证据 / 样本量不足 / 弱证据 / 中等证据`。当前 251 期数据下，**所有策略自动落在"样本量不足"**，页面必须如实显示，且**禁止**任何形式的"升级为有效"人工覆盖 |

#### L4 文案层防线（措辞规范，机器辅助检查）

- **数值必须带口径**：任何命中/概率/期望数字旁边必须能指出它的样本区间与样本量；孤立数字一律视为文案缺陷；
- **禁止绝对化断言**：延续 3.1 节收紧后的表述（"模型假设下期望相同"而非"已证明彩票随机"），L1 审计对"已证明""必然""绝对"等词做提示级检查；
- **禁止事后正确性叙事**：见 17.3 第 4 层第 5 条（"上期命中回顾"必须有随机对照组陪同，否则不得出现）；
- **每个数字都要有出处**：页面上的每个统计量，必须能在 `report.json` 里找到对应字段；硬编码数字（尤其是"经验数字"）视为缺陷，L1 审计会扫描页面里出现的百分比数字并要求它们来自报告数据。

> 这一层的现实意义：本项目最大的长期风险不是被同行攻击，而是**自己人**——未来的自己在某次迭代里，为了"页面好看一点"而把一句免责声明删掉、把一个数字硬编码、把一组号码加上高亮。L1~L4 的存在就是为了让这种改动**在构建时就红掉**，而不是等三个月后被人截图发到社区上。

### 17.6 版本路线更新（V3 之后的排序）

```
V3（当前待办，见 17.5 L1/L2 与第十七节）
   ├─ scripts/audit-safety.js + scripts/verify-data.js（第零道防线）
   ├─ 违规词表 / 交互审计 / 免责声明审计接入构建门禁
   ├─ 数据自洽性校验 + 数据指纹 + 抽样核对表
   └─ 证据等级自动标注字段
V4（本方案，预测功能）
   ├─ 数字体检报告 / 遗漏几何分布工具 / 形态诊断器
   ├─ 命中概率表 + 预测悖论演示（组合数精确计算）
   └─ 下一期观测（受控预测界面：3 规则组 + 1 随机对照组 + 三重指标）
V4.5
   ├─ 资金模拟器参数化（初始本金 / 每期注数可调）
   ├─ 传播素材导出（擂台图 / 收敛曲线静态图片）
   └─ 数据源核实与替换（**优先级最高的遗留项**），核实通过后才谈自动化管道
V5
   ├─ 第二种彩票（大乐透）复用 LotteryGame 抽象
   ├─ ML 模型接入（走同一套 Walk-Forward + 盲测 + FDR 校正，不得例外）
   └─ Agent Skill 接口 / 模板化战报（先模板，后 LLM）
```

**排序理由**：安全防线排在预测功能之前，不是为了保守，而是因为**先有防线，后面才敢放开做功能**。反过来（先做功能、后补防线）几乎必然导致"这个功能都做好了，就别撤了吧"的妥协，而妥协一旦发生一次，后面就不再有底线。

### 17.7 验收标准（可执行、可复现，不靠感觉）

V4 完成的定义：

1. `node scripts/audit-safety.js public/index.html src` 退出码为 0，且报告里列出全部检查项与通过情况；
2. 故意在 `src/build-site.js` 里插入一个"立即购买"按钮 → 重新构建 → **必须构建失败**（这条是防线的真实性测试，必须留下记录）；
3. 故意把某期红球改成 34 → `verify-data.js` 必须失败并指出具体期号；
4. 故意把 `calculatePrize` 的六等奖条件改错 → 奖级全枚举自检必须失败；
5. `report.json` 里每个策略都带 `evidence_level` 字段，且当前数据下全部为"样本量不足"；
6. 页面上不存在任何"购买/投注/下单"类可点击元素（人工复核 + 审计脚本双重确认）；
7. 下一期观测界面在**没有任何 JS 交互**的情况下（禁用 JS 打开页面），仍能读到"这 4 组号码统计上无差异"这句结论——**结论不能依赖交互才出现**，否则会有人只截图到号码那一半。

### 17.8 风险表更新（接第十二节）

| 风险 | 对策 |
|---|---|
| 加入"预测"功能后，产品性质从实验室滑向预测站 | 17.5 L1 自动审计 + 17.3 第 4 层硬性禁止清单；"预测"只允许出现在方法论语境，且逐处人工复查 |
| 用户把"隔空打靶"的 3 组号码当作推荐去买 | 每组必须并排显示 vs 随机分位数 + 证据等级；随机对照组永远同屏；禁用任何"推荐/主推"视觉强调；页脚常驻"本页号码不构成任何建议" |
| "上期预测命中 N 个"被当成效果证明 | 该叙事必须与随机对照组同屏出现，标题限定为"上期各组表现（含随机对照组）" |
| 安全防线随时间被削弱（文档管不住代码） | 底线全部落成构建门禁（L1/L2 硬失败）+ 定期用 17.7 第 2/3/4 条做"防线真实性测试" |
| 审计脚本本身被绕过（例如直接手改 public/index.html 后提交） | 审计必须在构建脚本末尾执行且产物不可手工修补（CI/部署命令串联三步构建）；审计报告带构建时间戳 |
| 数据源不可核实，却因为功能变多而被淡忘 | L2 数据指纹 + 抽样核对表 + 页面常驻说明；数据源核实写为 V4.5 最高优先级 |
| "证据等级"字段被人工改成"有效"以美化页面 | 该字段由 build.js 自动推导，L1 审计检查页面上出现的等级字符串必须在报告数据中存在 |
| 遗漏几何分布工具被反向解读为"越久没出越该买" | 工具必须同时输出"该遗漏长度的历史实际发生次数"与"按几何分布的理论期望次数"，用对比把"正常"讲清楚 |
| 形态诊断器被当作"过滤出更容易中的组合" | 必须同屏显示"过滤后组合数"与"概率不变"的对照；任何形态过滤都不得标注为"优化" |

### 17.9 V4 修订记录

- 新增第十七节：受控"预测"功能设计（数字体检 / 遗漏几何分布 / 形态诊断 / 下一期观测 / 预测悖论演示）。
- 新增四层安全底线（合规红线 / 数据层 / 统计层 / 文案层），并把其中可自动化的部分明确落成构建门禁脚本（`scripts/audit-safety.js`、`scripts/verify-data.js`）。
- 明确"预测"一词的解禁范围：允许用于方法论与自我检验语境，**禁止用于号码结论**；并给出逐处上下文检查的审计规则。
- 明确三条永久否决项（推荐号码 / AI 研判号码 / 生辰八字类输入），避免后续版本反复讨论。
- 新增"预测悖论演示"作为 V4 传播主素材，要求所有概率与期望由组合数精确计算，禁止硬编码经验数字。
- 统计层新增 FDR 多重比较校正、样本量下限自动标注、证据等级自动推导三个字段化规则。
- 将数据层从"清洗脚本顺带校验"升级为独立硬门禁（含奖级函数全枚举自检、组合层概率自检、数据指纹、官方抽样核对表）。
- 更新版本路线为 V3（防线）→ V4（受控预测）→ V4.5（资金模拟参数化 + 数据源核实）→ V5（多玩法 + ML + Skill）。

### 17.10 V4 第零步实施记录（本次已落地，与方案的对应关系）

本节记录"哪些是纸面设计、哪些已经真的跑起来了"，以免后续版本把设计误当成实现。

**已落地（有代码、有产出、有失败测试）：**

| 方案条目 | 落地位置 | 实测结果 |
|---|---|---|
| 17.5 L1 合规红线层 | `scripts/audit-safety.js` | 7 项检查全部通过；注入"立即购买"按钮后 exit 1（违禁话术 + 交易交互双项 fail） |
| 17.5 L2 数据层防线（硬校验） | `scripts/verify-data.js` | 格式/奖级全枚举/组合概率三项硬校验通过；改错红球范围、期号重复、奖级函数错误均 exit 1 |
| 17.5 L2 数据层防线（软校验 + 指纹 + 抽样表） | 同上 → `data/verification.json` | 红球卡方 36.45（p=0.1370）、蓝球 9.40（p=0.5205）；指纹 `af15e734…`；抽样核对表已上页 |
| 17.5 L3 统计层（证据等级 / FDR / 样本量下限） | `src/build.js` | 三个策略整体与开发集均为"无证据"，盲测集均为"样本量不足"；FDR 校正 q 值已生效，`is_significant` 口径改为 q<0.05 |
| 17.3 第 5 层 命中概率表 / 预测悖论 | `src/build-site.js`（数据来自 `verification.json`） | 总组合数 17,721,088、中奖概率 6.7095%、单注期望回报 0.4863/0.7402/0.9942 元全部上页；形态过滤器演示 1,107,568 → 23,109 组 |
| 17.5 L4 数字出处 | `scripts/audit-safety.js` 第 7 项 | 页面所有百分比均可追溯到 `report.json` 中的数值或其两两比值 |
| 防线真实性测试 | 17.7 第 2/3/4 条 | 4 项注入测试全部按预期失败（见 README 表格） |

**尚未落地（下一版继续）：**

- 17.3 第 4 层：**"下一期观测"受控预测界面**（3 组规则候选 + 1 组随机对照组 + 三重指标）。这是用户最初想要的那个功能，也是唯一必须等前三层就位之后才能做的功能——否则它只会变成一个看起来更专业的选号器。
- 17.5 L3 中"盲测集样本量下限"带来的一个副作用需要留意：按现规则，盲测集在累积到 100 期之前永远不会显示任何证据等级。这是**有意选择**（宁可不给结论，也不给一个会被误读的结论），但页面文案必须把"为什么这里长期是灰色"讲清楚，否则读者会以为是页面坏了。
- 17.3 第 1~3 层已完成（见 17.12），但形态诊断器目前只支持手动输入与"生成一组演示号码"，还没有做"把用户输入的号码与历史同形态组合的比例画成图"。
- 资金模拟器参数化（初始本金 / 每期注数可调）、传播素材导出，仍是 V4.5 的欠账。

**实施中发现的两个值得记下来的教训：**

1. **审计脚本本身会成为误报源，而误报的结局是被关掉。** 初版违禁词检查把页面里正确的免责声明（"不提供售彩、代购、充值、返利"）判成违规；初版"确定性检查"把页面上一句科普文案（"回测模式下完全不使用 `Math.random()`"）判成违规代码。两处都改成了上下文感知判定（否定语境 / 字符串字面量识别）。**一个天天误报的检查，最终一定会被人注释掉——那比不做检查更危险**，因为它制造了"我们有防线"的错觉。
2. **"同数据同算法同结果"这条原则，对校验脚本自己同样适用。** 蒙特卡洛卡方检验的伪随机序列一度写成"先取输出再推进状态"，第一项会直接返回种子本身、整个序列随种子平移（实测表现为同一命令两次运行给出 0.1515 和 0.1370）。已修正为"先推进再取输出"，并新增一条自检：同一检验重复运行必须逐位一致。

### 17.11 数据源核实（V4.5 已完成）与一次自我纠错

**结论：`gudaoxuri/lottery_history` 真实存在、每日更新、与本地数据零差异。此前"该数据源未能核实、无法确认是否存在"的结论是错的。**

核实过程（2026-09-13）：

| 环节 | 方法 | 结果 |
|---|---|---|
| 仓库存在性 | GitHub REST API `GET /repos/gudaoxuri/lottery_history` | HTTP 200；公开、未归档；描述"彩票历史数据收集器"；TypeScript |
| 维护活跃度 | 查询最近提交 | 最近一次数据更新提交 2026-09-13T00:43:16Z，GitHub Actions 每日自动更新 |
| 格式兼容性 | 拉取 `data/ssq.json` | 字段与本地 `ssq_source.json` 完全一致，可直接比对 |
| 逐期交叉核对 | 本地 251 期 vs 上游逐期逐号（期号/红球集合/蓝球/日期四项全等） | 251 期一致，**0 处不符**，本地独有期 0 |
| 新增期双源核对 | 上游新增 26105 vs 第二个独立来源 | 金华日报数字报《中国福利彩票开奖公告（第2026105期）》：红球 30 15 4 2 14 13、蓝球 8——**逐位一致** |
| 公告内部算术自检 | 用公告自身的销售额与六等奖注数复算 | 销售总额 333,874,688 元（合 166,937,344 注）、六等奖 14,332,306 注占 8.5854%，与奖级规则量级一致（不依赖第三方） |
| 入库 | `fetch-data.js --peer … --apply` | 已写回，数据从 251 期 → **252 期**，旧版自动备份 |

**教训（比结论本身更重要）**：之前那次"核实"只做了一次关键词搜索，搜索结果里没出现该仓库，就据此下了"仓库不存在"的结论，并把结论写进了页面与 README。**这是本项目风险表里"未验证的第三方数据源"那一行的镜像错误——拿不足的证据下了确定的结论。** 正确做法本该是：搜索只是线索，判定"资源是否存在"要直接查 API、直接尝试取用。此教训已同步写进页面（"本站曾经写错的一件事"表格）与 README，**留痕不删**：一个以讲证据为定位的项目，自己出的错更该留下痕迹。

**仍然做不到的事**：251 期历史数据仍是单一来源，未逐期与官方比对；自动校验只能证明内部自洽，不能证明逐期都对。抽样核对表已上页，供任何人复核。

**新增脚本 `scripts/fetch-data.js`**：抓取（3 次重试）→ 双方形状校验 → 逐期逐号交叉核对 → 新增期与第二来源比对 → 全部通过才写回（写入前自动备份）。**两个来源不一致时直接报警停线，不挑一个继续跑**（方案第六节）。至此方案第十二节风险表里"数据源反爬/容灾""未验证的第三方脚本"两行有了可执行对策。

### 17.12 自动化更新与 V4 第 1~3 层实施记录（本次已落地）

**一、开奖后自动更新（解决"每次开奖记得更新"这件事）**

双色球固定周二/周四/周日开奖（可用数据里最近 40 期的星期分布验证：13/14/13）。两条路径：

| 路径 | 实现 | 门禁 |
|---|---|---|
| GitHub Actions | `.github/workflows/update-lottery.yml`，按开奖时间排三个窗口（北京 21:40 / 次日 00:00 / 次日 08:40），周三五六各补跑一次覆盖延迟与补录 | 抓取→硬校验→重算→生成→冒烟→审计，任一步失败即中止，**不提交不发布** |
| 本机一键 | `scripts/update.ps1`（含 `-NoFetch` 离线重算、`-Peer` 第二来源复核） | 同上，七步全绿才算通过 |

两个专门加的静默失败防御：① **没有新期时若数据文件却变了 → 直接失败**；② **抓完之后核对最新期号是否真的变化**，防止"跑完了但什么都没抓到"。

**二、17.3 第 1~3 层已落地**

- 数字体检（Wilson 区间，非正态近似——正态近似会算出负数边界，页面上出现"−3% 的出现率"就没人信了）
- 遗漏分布（正面回答"该出了吧"：33 个号码 × 252 期 = 8316 次机会下，19 期的遗漏本来就该出现约 183 次）
- 形态诊断（任意 6 个红球的奇偶/大小/三区/连号/和值，外加"有多少组号码和它长得一样"）
- 计算逻辑统一放在 `src/number-health.js`，Node 与浏览器共用同一份公式，避免两端算出两个数字。

**三、本层最重要的设计：防误读装置**

实测数据里有 2 个红球（24、26）落在 95% 区间之外。为了不让它被读成"发现异常号码"，用蒙特卡洛算了纯随机基线并**并排展示**：

```
完全随机的数据里，33 个号码在 95% 水平上平均也有 1.793 个落在区间外（理论 1.65）
出现"至少 2 个号码偏离"的概率 = 56.2%（2000 次模拟）
```

**结论：这个"发现"是完全随机时最正常的结果之一。** 这是多重比较陷阱的一次现场演示，也是本项目最有说服力的一块内容——它演示的不是彩票随机，而是**人如何从随机数据里看出并不存在的规律**。

**四、新增第五道门禁 `scripts/smoke-test-client.js`（本次最重要的工程改进）**

补的缺口：页面交互代码是**内联进 HTML** 的，平时没有任何东西执行它——写错变量名，构建全绿、审计全绿，用户点下去却什么都没发生。该脚本用极简 DOM 桩把页面里的 `<script>` 抽出来真跑一遍，再模拟点击与输入（25 项检查）。

**它不是理论上的保险，第一次运行就抓到一个真 bug**：`window.LotteryTools` 未挂上（顶层 `var` 创建的全局绑定与执行环境里读 `window.X` 不总是同一个东西），三个工具会在真实浏览器中**全部静默失效**，而构建与审计全绿。已修复并把教训写进 `src/number-health.js` 注释。

**这条教训的普遍形式值得写进方法论**：**"构建通过"与"功能可用"是两件不同的事。** 一个只在构建期跑的校验体系，天然看不见运行时的问题；凡是"产物里嵌了代码"的项目，都应该有一条真的去执行它的检查。

**五、另一个实测发现：单域名抓取本身就是静默失败源**

本机 Node 拉 `raw.githubusercontent.com` 频繁 ENOTFOUND / ECONNRESET（同一时刻 PowerShell 却能拉到，说明是 Node 这条链路的问题）。`fetch-data.js` 已改为**多入口轮询**（api.github.com contents API 优先、raw 备用，各自带 3 次重试），并把"实际用了哪个入口"打进日志——这样"某天开始一直走备用入口"是可见的，而不是悄悄降级。失败错误信息也做了兜底：`ECONNRESET` 的 `message` 是空串，直接打印会得到"第 1/3 次抓取失败："后面什么都没有，那种日志等于没有日志。
