name: diag-arrow → test
# 诊断：workflow 的 name 里含 U+2192（→）时，GitHub 是否能正常执行？
# 结论出来后本文件删除。

on:
  push:
  workflow_dispatch:

jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: echo "arrow-name workflow executed"
