# 彩票数据 / AI 概率实验室 —— 完整产品与技术方案

> 综合你自己的分析、Gemini 的方案、以及你对 Gemini 方案的修正，整理成的一份可直接执行的完整方案。

---

## 一、项目定位（一切设计的前提）

### 1.1 核心定位

**不做**：AI/算法预测彩票中奖号码的工具。
**做**：一个用真实开奖数据，持续检验"选号策略/算法/AI 是否真的比随机数强"的公开实验平台。

一句话定位：

> **"随机数打脸实验室"——用 1000+ 期真实数据，让所有选号玄学、统计策略、机器学习模型，接受同一个残酷的对手：大数定律。**

**项目北极星原则（写进方法论页面，所有排行榜条目都要遵守）：**

> **任何策略，在没有通过盲测之前，都不准自称"有效"。**

即便某个模型连续很多期表现亮眼，展示时也要同时给出"历史开发集表现"和"盲测集表现"两个数字，并明确标注当前证据是否足以支持"优于随机"这个结论——不够就写"不够"，不要因为好看就含糊过去。

### 1.2 命名与文案原则

- 避免使用"预测""稳赚""精准""内部号码""VIP"等词。
- 建议命名方向：`XX概率实验室` / `XX随机数实验室` / `算法擂台` / `Number Lab`，不要出现"预测网"字样。
- 首页必须有清晰声明：本项目为统计与算法科普实验，不提供售彩、代购、充值、返利等服务；彩票开奖为独立随机事件，历史数据不影响未来结果。

### 1.3 合规边界（写死在产品规则里，不要触碰）

- 不接入下注、代购、充值、拼团购彩、返利。
- 不承诺、不暗示、不使用中奖话术（"高命中率""专家推荐""保底"等）。
- 国内对互联网售彩管控严格，未经批准不得通过互联网销售彩票；官方也多次提示"AI预测彩票号码"没有科学依据。产品必须始终停留在"数据分析 + 算法科普"层面。
- **生成的号码、资金曲线旁绝对不能出现"立即购买""一键下单""复制号码去投注"这类交互按钮**——一旦出现，产品性质就从"实验"滑向"售彩导流"，这是最容易被忽视但最不能碰的一条线。

---

## 二、产品形态：网页 vs Agent Skill，怎么选

**结论：先做静态网页，Skill 作为第二入口后置接入，不要一开始就做 Skill，也不要做"纯随机点击生成"的网页。**

原因：

| 维度 | 纯随机静态页 | 每次现算的 Agent Skill | 推荐方案：算法引擎 + 回测 + 网页（AI 后置） |
|---|---|---|---|
| 可复现性 | 有，但无意义 | 差（每次结果可能不同） | 好（同数据+同算法=同结果） |
| 成本 | 极低 | 每次调用都要花 token/等待 | 低（AI 只在开奖后跑一次批处理） |
| 内容/传播价值 | 低，同质化严重 | 中 | 高（回测曲线、排行榜天然适合传播） |
| 技术含量体现 | 低 | 中 | 高（数据处理+统计+回测+可选ML） |
| 用户回访理由 | 几乎没有 | 一般 | 强（下一期开奖→验证上期预测→更新排行榜，天然循环） |

原则：**算法负责计算（确定性、可复现），AI 只负责"解释结果"和"生成战报"，不负责"决定号码"。** Skill 之后可以作为"用自然语言查询当前排行榜/生成个性化策略解读"的交互层加上去。

---

## 三、数学基础（必须先想清楚，否则后面的"回测""排行榜"都是假的）

这是整个项目的地基，比 UI 和 AI 都重要。

### 3.1 期望值是固定的，这是产品最大的看点而不是缺陷

以双色球为例：红球 33 选 6，任意一组固定选号，在开奖真正均匀随机的前提下：

```
单期红球命中期望 E = 6 × (6/33) ≈ 1.0909 个
```

大乐透前区 35 选 5：

```
单期前区命中期望 E = 5 × (5/35) ≈ 0.7143 个
```

**结论**：在"开奖机制公平、每期独立同分布"这个模型假设下，只要选号策略在开奖前就已确定、不使用未来信息，那么无论用随机、冷热、遗漏、贝叶斯、XGBoost 还是 LLM，长期平均命中数理论上都会趋近同一个期望值。**这不是产品的 bug，而是产品最核心的科普内容**——"用真实数据持续检验，当前样本是否呈现出与这个模型明显不一致的证据"。注意文案上不要写成"彩票已经被数学证明完全随机"这种绝对化断言：我们能验证的是模型假设下各策略期望是否一致，不是对开奖机制本身下结论。这个措辞收紧看起来学究，但恰恰是这类项目"讲科学"人设最容易被挑理的地方。

### 3.2 不要说"回测 300 期就一定收敛"——要展示置信区间

有限样本下，即使长期期望相同，短期波动依然可能很大。正确做法是把"收敛过程"和"波动区间"都做成可视化内容，而不是简单地宣称"都一样"。

给每个策略配置：

```
平均命中数 = 样本均值
理论期望值 = 1.0909（红球）/ 0.7143（前区）等
95% 置信区间 = 样本均值 ± 1.96 × (样本标准差 / √期数)
```

页面上直接展示："当前 XX 策略平均命中 1.094，高于理论期望 0.003，但差异没有统计显著性（落在 95% 置信区间内）"。这比单纯甩一个数字有说服力得多，也更真实。

### 3.3 回测必须是 Walk-Forward，禁止数据泄漏

**错误做法**（有数据泄漏风险）：

```
用全部 1000 期历史数据 → 计算热号/冷号/权重 → 拿这套权重去"预测"这 1000 期里的每一期 → 统计命中率
```

这样算法其实"看到了未来"，回测结果没有意义。

**正确做法（滚动预测 / walk-forward）**：

```
用第 1~100 期  → 计算策略 → 预测第 101 期 → 与真实第101期开奖比较 → 记分
用第 1~101 期  → 计算策略 → 预测第 102 期 → 与真实第102期开奖比较 → 记分
用第 1~102 期  → 计算策略 → 预测第 103 期 → ……
```

即：`D[1:t] → 生成第 t+1 期候选号码 → 与真实第 t+1 期结果比较 → 累计成绩`。所有策略、包括未来要加的机器学习模型，都必须走这套流程，否则排行榜是假的。

### 3.4 防止"调参调出假优势"（多重比较 / 数据窥探问题）

如果你尝试了成百上千种参数组合，即使彩票完全随机，也几乎必然能"挑出"一个历史表现特别好的组合——这不是策略真的有效，而是选择性偏差。

对策：把数据严格分成三段，并且写进产品规则公开展示：

```
训练/开发集：用于设计和调整策略（例如前 60% 的历史期数）
验证集：用于筛选/调参（中间 20%）
最终盲测集：只用来做一次性最终评估，绝不能拿来反过来调参（最后 20%）
```

这一条建议直接写在网站的"方法论"页面里，作为你这个项目"讲科学"的证据，也是和一般"预测网站"最大的差异化。

### 3.5 比较"策略 vs 随机"要用配对差值检验，而不是分别看置信区间

与其分别看"策略的 CI 是否覆盖理论期望 1.0909"，不如在**同一期**上直接比较策略与随机基准的差值——因为每一期的真实开奖对所有策略都是同一个，天然构成配对数据：

```
D_i = H_策略,i − H_随机,i   （第 i 期，策略命中数 − 随机基准命中数）
```

对 {D_i} 做配对 t 检验（或更稳健的非参数符号秩检验），看均值是否显著偏离 0，比"各自的 CI 是否盖住 1.0909"更直接回答"这个策略到底有没有比随机强"。

**一个需要留意的细节**：滚动窗口的训练数据是逐期重叠的（第 101 期用第 1~100 期，第 102 期用第 1~101 期），有评审担心这会破坏样本独立性。严格来说，只要真实开奖本身逐期独立，命中数 D_i 之间的独立性并不会因为训练窗口重叠而被破坏（因为 D_i 只依赖于当期这一次独立开奖，跟策略是怎么算出来的无关）；但为了不过度依赖"真实开奖严格独立同分布"这个假设，**建议把 t 检验放在内部校验层，用户界面上优先展示 3.6 节的蒙特卡洛分位数**——它不需要假设正态分布，也更容易向非统计背景的用户解释。排行榜可以两者都存，但对外只讲分位数。

### 3.6 随机基准做成一条分布：固定真实开奖 + 生成大量独立随机策略

具体方法要说清楚，避免歧义——**不是打乱历史开奖顺序，而是固定住真实开奖序列不变，生成 5,000~10,000 条相互独立的"随机选号策略"，让它们都跑一遍同样的 Walk-Forward 流程**，得到一条经验分布：

```
random_run_0001 → 平均命中 1.088
random_run_0002 → 平均命中 1.102
random_run_0003 → 平均命中 1.075
...（5000~10000 条）
```

再把你真正要评估的策略（热号/综合/未来的 ML 模型）的得分插进这条分布里，直接读分位数。这样"综合策略跑赢了 73% 的随机策略"这句话就有了一个很好玩的潜台词——**它本质上只是恰好落在了随机分布的第 73 百分位**，这正是整个项目想讲的故事：听起来牛，其实就是运气分布里的普通一员。

这一节和 3.5 都不是要推翻 3.1~3.4 的期望值/置信区间框架，而是补一层"两两比较"的严谨性——长期均值+CI 给"绝对水平"的直觉，蒙特卡洛分位数给"是否真的更强"的结论。

### 3.7 不要只用"命中数"当唯一指标，分层展示

一个策略"好不好"不能只看平均命中数，建议从第一天就设计好分层指标，方便以后往上叠：

```
Level 1 基础命中：平均红球命中、蓝球命中率
Level 2 极端事件：≥4红/≥5红/6红 出现次数、一等奖组合次数
Level 3 命中分布：命中0~6个红球分别的次数分布（不是只看均值）
Level 4 资金层面：累计投入、累计奖金、ROI、最大回撤、期末余额
Level 5 统计层面：vs 理论期望、vs 随机蒙特卡洛分位数、显著性/证据等级
```

否则容易陷入"策略A平均1.095比策略B平均1.091好"这种误导性比较，而两者的命中分布可能完全不同（比如一个稳定在2左右，另一个偶尔中5个但大部分时候0个）。

---

## 四、系统架构

```
                    官方/公开历史开奖数据
                            ↓
                  ┌──────────────────┐
                  │   数据处理层      │  清洗 / 结构化 / 存成 JSON
                  └────────┬─────────┘
                            ↓
                  ┌──────────────────┐
                  │   策略/算法层     │  随机、热号、冷号(遗漏)、
                  │                  │  奇偶/分区/和值、加权综合、
                  │                  │  （后期可选）ML 模型
                  └────────┬─────────┘
                            ↓
                  ┌──────────────────┐
                  │  Walk-Forward     │  滚动预测 + 记分
                  │  回测引擎         │  统计均值/方差/置信区间
                  └────────┬─────────┘
                            ↓
                  ┌──────────────────┐
                  │   排行榜/统计层   │  收敛曲线、显著性检验、
                  │                  │  短期爆点(命中4+/5+次数)
                  └────────┬─────────┘
                            ↓
                  ┌──────────────────┐
                  │  AI 解释层(可选)  │  离线批处理生成"战报"，
                  │                  │  不参与号码生成决策
                  └────────┬─────────┘
                            ↓
                    静态网页（Cloudflare Pages）
                            ↑
                  GitHub Actions 定时任务
                  （开奖后自动抓数据→重算→提交→自动部署）
```

**部署形态：** 完全静态、零服务器常驻成本。

```
GitHub 仓库（数据 + 算法代码）
    ↓ 定时触发（开奖后）
GitHub Actions：抓取最新一期数据 → 跑回测 → 更新 JSON → （可选）调用一次 LLM 生成战报 → commit
    ↓ push 触发自动构建
Cloudflare Pages：静态资源 + 预生成 JSON，浏览器端 JS 直接计算"我的策略"结果
```

好处：前期完全不需要后端服务器/数据库；用户交互式生成（拖动权重滑块）在浏览器端用 JS 实时算，0 API 成本、0 延迟。

---

## 五、功能模块清单（严格按优先级开发）

1. **历史数据 + Walk-Forward 回测引擎**（地基，必须先做对）
2. **基础策略**：纯随机 / 热号 / 冷号(遗漏) / 奇偶+分区+和值综合 / 自定义加权
3. **统计展示**：收敛曲线、理论期望基准线、置信区间、显著性提示、排行榜
4. **短期"爆点"数据**：近10/30/100期表现、历史单期最高命中、命中4+/5+次数——这是内容传播的关键素材
5. **视觉与交互**（赛博/极客风格，开奖号码动画等）
6. **"我的策略"**：用户拖动权重滑块（冷热倾向/遗漏倾向/随机扰动等），前端实时生成号码。**这里要显式拆成两个数字**：一个是"历史回测成绩"（用户可以随便调权重、反复看这个数字，仅供娱乐，明确标注"不作为策略有效性证据"）；另一个是"盲测成绩"（用一段用户调参时完全看不到、无法拿来调整的历史区间跑出来的成绩，调参过程中始终隐藏，最后才展示）。否则用户会把"历史回测调到全站第一"当成"我发现了规律"，这其实只是数据窥探的一种用户端形式。
7. **AI 战报**：开奖后离线批处理生成一段"点评"，解释各策略本期表现，语气可以幽默、强调"没有显著跑赢随机"这一核心事实
8. **（后期）Agent Skill 接口**：例如 `/lottery 双色球` 自动拉取最新排行榜、按用户描述的偏好生成一组号码并附带统计学解释

**注意开发顺序**：先做 1→2→3，把回测引擎和统计学基础做对；4→5→6 是产品体验和传播点；7→8（AI 相关）放在最后，且第一版 AI 战报完全可以用模板文案代替，不调用任何模型。

**V2 补充模块**（来自后续评审，建议插入到对应阶段）：

- **长期资金模拟（对外正式名称，"韭菜模拟器"作为内部彩蛋模式名）**：给每个策略配一个虚拟账户，按真实奖级规则结算奖金，画出账户余额随开奖期数变化的曲线。奖级判定（命中几个红球+是否中蓝球 → 第几等奖）和实际奖金金额（一二等奖是浮动奖池，三等奖以下才是固定金额）要在数据结构里分开存，不要在展示层硬编码一个固定的"一等奖500万"，否则单期头奖会让曲线严重失真。默认参数建议做成可调（例如初始本金、每期投注注数），因为"1万元本金、每期1注"1000期下来只会缓慢下降，视觉冲击有限；调低本金或调高单期注数能更快看到"长期负期望"这条曲线，但具体倍率建议参考官方公开的奖级分布数据校准，而不是拍脑袋定，这条曲线的意义是科普"为什么长期负期望"，不是追求视觉刺激。
- **蓝球（特别号）必须建模**：双色球单中蓝球就有六等奖，不把蓝球和真实奖级规则接入回测，"资金曲线"和排行榜都是半成品，需要在阶段一的数据结构和回测引擎里就设计进去，而不是后补。
- **独立的"理论基准"层**：不要把"理论期望值 1.0909"混在"随机策略"这一项里，单独列一层 Benchmark，所有策略（含随机模拟、启发式规则、以后可能加的 ML 模型）都相对这条线比较，页面结构会更清楚。
- **版本号管理**：给数据集、算法规则各自加版本号，例如"数据集截至 2026-09-11 / Hot Strategy v1.1 / 回测区间第 X~Y 期"。因为算法规则以后一定会改（比如热号窗口从固定改成指数衰减），没有版本号的话，今天和两个月后的排行榜根本没法比较，也没法解释为什么某天排行榜数字突然变了。
- **自动生成传播素材**：GitHub Actions 每次重算后，顺手导出一张"本期擂台图/收敛曲线"静态图片，作为社媒/社区分享素材。这不是锦上添花，是这类数据/算法项目获取早期流量的现实手段，建议从阶段四就开始做。

---

## 六、数据结构设计（示例）

```
data/
 ├── ssq.json          # 双色球历史开奖
 ├── dlt.json          # 大乐透历史开奖
 ├── leaderboard.json  # 各策略累计回测成绩
 └── report_latest.json # 最新一期 AI/模板战报
```

单期开奖记录示例：

```json
{
  "period": "2026099",
  "date": "2026-09-10",
  "red": [5, 12, 17, 21, 28, 32],
  "blue": 7,
  "source": "gudaoxuri/lottery_history",
  "fetched_at": "2026-09-12T08:00:00Z"
}
```

`source` / `fetched_at` 用于多数据源交叉核对：如果两个数据源对同一期的开奖号码给出不一致的结果，正确做法是**暂停自动发布并报警，而不是随便挑一个继续跑**——抓错数据不被发现，比抓不到数据要恐怖得多。

策略打分中间结果示例（用于"为什么选中17号"这类可解释展示）：

```json
{
  "number": 17,
  "score": 0.67,
  "components": {
    "frequency": 0.71,
    "omission": 0.82,
    "recent_trend": 0.63,
    "zone_balance": 0.55,
    "random_noise": 0.19
  }
}
```

排行榜条目示例（加入版本号，避免规则改了以后排行榜无法对比）：

```json
{
  "strategy": "冷热综合",
  "strategy_version": "v1.1",
  "dataset_snapshot": "2026-09-11",
  "periods_tested": 1482,
  "mean_hit": 1.094,
  "theoretical_expectation": 1.0909,
  "ci_95": [1.072, 1.116],
  "vs_random_percentile": 63,
  "is_significant": false
}
```

单期回测结果示例（奖级判定和实际奖金分开存，一/二等奖是浮动奖金，三等奖以下是固定奖金）：

```json
{
  "period": "2026100",
  "predicted_red": ["05", "12", "17", "21", "28", "32"],
  "actual_red": ["05", "12", "19", "21", "28", "33"],
  "predicted_blue": "07",
  "actual_blue": "07",
  "red_hits": 4,
  "blue_hit": true,
  "prize_level": 5,
  "bonus_type": "fixed",
  "bonus": 10
}
```

---

## 七、关键算法示例（伪代码/JS）

**核心原则：两条评审都指出的问题必须先修——① 回测循环里"预测第几期"的索引不能错（off-by-one）；② 回测用的策略必须是确定性的或用固定种子的伪随机数，禁止裸用 `Math.random()`，否则同一份历史数据每次跑出的排行榜都会变，直接违背"同数据+同算法=同结果"的原则。**

约定两种模式：

```
交互模式（用户点"再来一组"）→ 允许普通 Math.random()，每次可以不同
回测模式（生成排行榜/收敛曲线）→ 必须用「期号 + 策略名」做种子的伪随机数，
                                 或者干脆用不含随机成分的确定性规则（如 Top-N）
```

### 7.1 种子伪随机数生成器（回测专用，保证可复现）

```javascript
// Mulberry32：轻量、够用的种子随机数生成器
function seededRandom(seed) {
  let a = seed;
  return function () {
    a |= 0; a = (a + 0x6D2B79F5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function hashSeed(str) {
  let h = 0;
  for (let i = 0; i < str.length; i++) h = (Math.imul(31, h) + str.charCodeAt(i)) | 0;
  return h;
}
// 用法：const rng = seededRandom(hashSeed(`${strategyName}-${period}`));
```

### 7.2 多指标归一化后再加权（避免不同量纲直接相加）

```javascript
// 先归一化到 0~1，再加权，不要把频次和遗漏期数直接相加
function normalize(valuesMap) {
  const values = Object.values(valuesMap);
  const min = Math.min(...values), max = Math.max(...values);
  const range = max - min || 1;
  const result = {};
  for (const [k, v] of Object.entries(valuesMap)) {
    result[k] = (v - min) / range;
  }
  return result;
}
```

### 7.3 确定性基准策略（回测用，例如"热号 Top-6"，无随机成分）

```javascript
// 严格确定性：按分值降序排列，分值相同按球号升序打破平局
function hotStrategy(trainData, windowSize = 50) {
  const windowData = trainData.slice(-windowSize);
  const freqMap = {};
  for (let i = 1; i <= 33; i++) freqMap[String(i).padStart(2, "0")] = 0;

  windowData.forEach((draw) => {
    draw.red.forEach((ball) => { freqMap[ball] = (freqMap[ball] || 0) + 1; });
  });

  const sortedBalls = Object.keys(freqMap).sort((a, b) => {
    if (freqMap[b] !== freqMap[a]) return freqMap[b] - freqMap[a];
    return Number(a) - Number(b); // 平局按球号升序，保证结果唯一确定
  });

  const blueFreq = {};
  windowData.forEach((draw) => { blueFreq[draw.blue] = (blueFreq[draw.blue] || 0) + 1; });
  const bestBlue = Object.keys(blueFreq).sort((a, b) => blueFreq[b] - blueFreq[a])[0] || "01";

  return {
    red: sortedBalls.slice(0, 6).sort((a, b) => Number(a) - Number(b)),
    blue: bestBlue,
  };
}
```

### 7.4 带权重的策略（交互模式用普通随机，回测模式传入种子 rng）

```javascript
// rngFn 由调用方传入：交互模式传 Math.random，回测模式传 seededRandom(...)
function generateRedBalls(weightsMap, rngFn = Math.random) {
  const balls = Array.from({ length: 33 }, (_, i) => String(i + 1).padStart(2, "0"));
  const selected = [];
  const w = { ...weightsMap };

  for (let i = 0; i < 6; i++) {
    const total = balls.reduce((sum, b) => sum + (w[b] || 1), 0);
    let r = rngFn() * total;
    for (const ball of balls) {
      r -= w[ball] || 1;
      if (r <= 0) {
        selected.push(ball);
        balls.splice(balls.indexOf(ball), 1);
        break;
      }
    }
  }
  return selected.sort((a, b) => Number(a) - Number(b));
}
```

### 7.5 Walk-Forward 回测主循环（已修正 off-by-one，并加入数据顺序防御）

```javascript
/**
 * history.slice(0, t) 取到的是下标 0 ~ t-1，即"前 t 期"。
 * 那么第一个还没被看过的期就是下标 t 本身，
 * 所以待预测目标必须是 history[t]，而不是 history[t+1]
 * （原方案这里写成了 t+1，白白跳过了一期，必须修正）。
 */
function walkForwardBacktest(rawHistory, strategyPredictFn, minTrainSize = 100) {
  // 【关键防御】国内大多数公开彩票数据源/API返回的是"最新一期在前"的倒序列表。
  // 如果不做这一步，slice(0, t) 拿到的其实是"未来"的数据去预测"过去"的期数，
  // 是比 off-by-one 更严重的数据穿越问题，必须在入口处强制排序。
  const history = [...rawHistory].sort((a, b) => Number(a.period) - Number(b.period));

  const records = [];
  for (let t = minTrainSize; t < history.length; t++) {
    const trainData = history.slice(0, t);   // 只能看到前 t 期
    const actual = history[t];               // 真实第 t+1 期（0-indexed 下标 t）

    const predicted = strategyPredictFn(trainData, actual.period); // 可用 actual.period 作种子

    const redHits = actual.red.filter((b) => predicted.red.includes(b)).length;
    const blueHit = actual.blue === predicted.blue;

    records.push({
      period: actual.period,
      redHits,
      blueHit,
      prize: calculatePrize(redHits, blueHit),
    });
  }
  return records;
}

// 双色球奖级判定：只判定"中了第几等奖"，不在这里决定具体金额
function calculatePrize(redHits, blueHit) {
  if (redHits === 6 && blueHit) return { level: 1, bonusType: "floating" }; // 一等奖，实际金额取决于当期奖池
  if (redHits === 6) return { level: 2, bonusType: "floating" };            // 二等奖，同样浮动
  if (redHits === 5 && blueHit) return { level: 3, bonusType: "fixed", bonus: 3000 };
  if (redHits === 5 || (redHits === 4 && blueHit)) return { level: 4, bonusType: "fixed", bonus: 200 };
  if (redHits === 4 || (redHits === 3 && blueHit)) return { level: 5, bonusType: "fixed", bonus: 10 };
  if (blueHit) return { level: 6, bonusType: "fixed", bonus: 5 };
  return { level: 0, bonusType: "fixed", bonus: 0 };
}

// 浮动奖金（一、二等奖）用官方公布的历史平均值做近似估算，不需要还原奖池分配算法
function estimateFloatingBonus(level, historicalAverages) {
  return historicalAverages[level] ?? 0; // historicalAverages 可用近几年官方公布的一/二等奖平均单注金额
}

function summarize(records) {
  const n = records.length;
  const mean = records.reduce((s, r) => s + r.redHits, 0) / n;
  const variance = records.reduce((s, r) => s + (r.redHits - mean) ** 2, 0) / (n - 1);
  const stderr = Math.sqrt(variance / n);
  return { meanHit: mean, ci95: [mean - 1.96 * stderr, mean + 1.96 * stderr] };
}

// 配对差值检验：strategyRecords 和 randomRecords 必须是同一段历史、逐期对齐
function pairedDiffTest(strategyRecords, randomRecords) {
  const diffs = strategyRecords.map((r, i) => r.redHits - randomRecords[i].redHits);
  const n = diffs.length;
  const meanDiff = diffs.reduce((a, b) => a + b, 0) / n;
  const variance = diffs.reduce((s, d) => s + (d - meanDiff) ** 2, 0) / (n - 1);
  const stderr = Math.sqrt(variance / n);
  const tStat = meanDiff / stderr; // 可再查 t 分布表或用近似正态得到 p 值
  return { meanDiff, tStat };
}
```

> 一、二等奖是浮动奖金，实际由当期奖池和中奖注数决定，不需要也不建议还原真实奖池分配算法，用官方历史公布的平均单注金额估算即可（见 `estimateFloatingBonus`）；三等奖以下是固定金额，直接写死没问题。这个拆分也是为了避免"资金曲线"因为个别期头奖数字失真——用近似值远比自己编一个固定的"500万"靠谱。

---

## 八、AI 的角色定位

**原则：AI 不决定号码，只解释已经算好的确定性结果。**

流程：

```
算法层输出结构化数据（各策略评分、命中统计、排行榜变化）
        ↓
（开奖后，离线批处理，一次性调用，非实时）
AI 读取这些结构化数据，生成一段自然语言"战报"
        ↓
静态网页展示战报
```

Prompt 设计要点：

- System 层面要求：语气可以幽默/略带调侃，但必须客观、基于给出的真实数字，不允许编造未提供的统计结论。
- 每次调用都必须明确告知"本期是否有策略显著跑赢随机"这一核心事实，避免暗示某策略"更准"。
- 输出长度限制在 150~250 字，避免变成一本正经的伪科学分析。

第一版建议：**完全不接 AI API，用模板文案代替战报**（例如根据数字自动填空生成句子），先把回测引擎和数据管道跑稳，再逐步引入 LLM，降低成本和不确定性。

---

## 九、开发路线：分阶段控制范围，第一版只做双色球

两条评审都指出了同一个风险：**方案讨论到这个阶段，很容易从"7天能跑起来的小项目"膨胀成"小型统计研究平台"**。对策是明确分阶段，并且**V0/V1 阶段只做双色球一种彩票**，把架构抽象成可复用的 `LotteryGame` 之后，再接大乐透等其他玩法——不要一开始就双色球、大乐透一起做，那样项目会变成"彩票数据维护工程"而不是"概率实验室"。

**V0（先验证核心结论，越丑越好，目标是几天内跑出真实数字）：**
- 双色球历史数据（哪怕只有近 500~1000 期）+ 蓝球 + 奖级规则，从一开始就设计进数据结构。
- 独立的"理论基准"层（Benchmark，1.0909）。
- 三个策略：随机、热号、冷号，且都是确定性或种子随机（参考 7.1/7.3 节代码）。
- Walk-Forward 回测引擎，注意 7.5 节的两个防御点：off-by-one 索引 + 按期号升序排序。
- 输出：平均命中数 + 收敛曲线。

跑到这一步就该先停下来问自己："这三个策略在真实历史数据上到底是什么成绩？"——只要这个数字出来了，后面的一切都是基于真实数据继续做，而不是脑补设计。

**V0.5（把统计严谨性叠上去）：**
- 配对差值检验 + 蒙特卡洛随机分布分位数（3.5/3.6 节）。
- 命中分布展示（不只是均值）。
- 排行榜、短期爆点数据（近10/30/100期表现）。

**V1（做成能玩的产品）：**
- 视觉与交互设计（赛博/极客风格、开奖动画）。
- "我的策略"权重滑块，历史回测成绩 + 盲测成绩分开展示（见第五节第 6 条）。
- 蓝球和奖级规则接入回测，做出长期资金模拟（"韭菜模拟器"作为彩蛋模式）。

**V1.5（自动化管道）：**
- GitHub Actions 定时抓取数据 → 重跑回测 → 更新 JSON → 提交；接入 Cloudflare Pages 自动部署。
- 抓取脚本必须做重试 + 多数据源容灾，并且严格核对多个数据源是否一致，不一致就停止自动发布并报警，而不是随便挑一个继续跑。
- **关于数据源**：网上能找到一些开源的历史开奖数据镜像仓库（例如 GitHub 上有维护"双色球/大乐透历史数据"JSON 文件的开源项目），可以作为起点，但字段结构、更新频率、准确性都需要自己核实，不要盲目相信任何一个来源（包括网上流传的现成脚本里写的 API 地址——有些看起来像官方接口，实际上是不存在或已失效的占位符，运行前务必自己验证能不能连通、返回的数据对不对）。
- 顺手导出一张"本期擂台图/收敛曲线"静态图片，作为传播素材。

**V2（AI 和更多玩法，最后再做）：**
- AI 战报（先模板文案，验证没问题后再接一次性离线 LLM 调用）。
- 机器学习模型（XGBoost/LSTM/Transformer 等）——不用急，等前面的基线策略都跑完、结论稳定以后再加，避免"为了 AI 而 AI"。
- 第二种彩票（大乐透等），复用 `LotteryGame` 抽象。
- Agent Skill 接口（`/lottery` 类查询）。

**Agent Skill 作为后续迭代**：待网页有真实数据积累和访问反馈后，再开发 `/lottery` 类 Skill，让用户可以用自然语言查询排行榜、生成个性化权重策略并附带统计解释。

---

## 十、内容与传播角度

这个项目最大的传播点不是"预测准不准"，而是"用真实数据把玄学打回原形"的过程本身。可用素材：

- "回测 1000+ 期：机器学习模型的命中率和随机数几乎一模一样" —— 收敛曲线截图。
- "算法打脸排行榜"：定期公布哪个策略本期"翻车"、哪个"暴击"，强调这只是短期波动。
- "方法论"页面公开 Walk-Forward 回测规则和训练/验证/盲测分割方式，作为"这不是玄学网站"的证据，适合在技术社区（V2EX、知乎技术区等）分享。

---

## 十一、商业化边界

**可以做的：**
- 展示广告（非彩票相关）。
- 高级统计功能（更长历史回测、自定义策略保存、多期对比）作为增值点。
- 内容衍生（数据可视化文章、方法论解读）积累流量。

**绝对不能做的：**
- 不能卖"预测号码"、VIP 精准版、专家推荐。
- 不能接入购彩、代购、合买、充值、返利等任何交易环节。
- 不能使用"保底""稳赚""内部消息"等误导性话术。
- 不能在生成的号码、资金曲线旁放"立即购买""一键下单"之类的交互按钮，即便只是链接到第三方购彩平台也不行。

---

## 十二、风险与对策清单

| 风险 | 对策 |
|---|---|
| 被误解为"教人买彩票"的灰产网站 | 首页/方法论页明确声明实验性质，不接入任何购彩环节 |
| 回测存在数据泄漏，结论不可信 | 严格执行 Walk-Forward 滚动回测，公开回测规则 |
| 调参调出"虚假优势"（数据窥探） | 训练/验证/盲测三段式分割，盲测集不参与调参 |
| 实时调用 LLM 成本高、延迟大、结果不可复现 | AI 只做开奖后离线批处理战报，号码生成由确定性算法完成 |
| 短期波动被误读为"策略有效" | 同时展示长期均值、置信区间、显著性判断，避免只放一个亮眼数字 |
| 域名/内容被误判为违规 | 避免"预测""稳赚"等敏感词，突出"实验室/统计科普"定位 |
| 数据源反爬/云端 IP 被限流封禁 | 优先用开放 API、开源镜像数据源；抓取脚本加重试 + 多数据源容灾，不要死磕官网反爬 |
| 文案过度宣称"已证明随机"，被挑理 | 统一表述为"模型假设下期望相同，且当前样本无显著背离证据"，避免绝对化断言 |
| 数据源返回"最新在前"的倒序列表，直接喂进回测会用未来数据预测过去 | 回测引擎入口处强制按期号升序排序，作为默认防御（见 7.5 节代码） |
| 用户在"我的策略"里反复调权重，把历史回测调到第一名当成"发现规律" | 历史回测成绩和盲测成绩分开展示，明确标注历史回测"仅供娱乐，不作为有效性证据" |
| 网上流传的抓取脚本里 API 地址是占位符或已失效 | 使用前先手动验证能否连通、返回字段是否符合预期，不要直接信任未验证的第三方代码 |

---

## 十三、技术栈建议

```
前端：原生 JS 或轻量框架（无需复杂框架），ECharts 做收敛曲线/排行榜可视化
数据：JSON 静态文件（历史开奖、回测结果）
算法：TypeScript/JavaScript 实现，全部跑在浏览器端或 GitHub Actions 构建时
自动化：GitHub Actions（定时抓取数据 + 重跑回测 + 提交）
部署：Cloudflare Pages（静态托管，免服务器）
AI（后置、可选）：任一 LLM API，仅用于开奖后离线批处理生成"战报"文案
```

第一版可以做到**纯 HTML + CSS + JS + 静态 JSON**，不需要任何数据库或常驻后端。

---

## 十四、开发优先级总结

1. 历史数据 + Walk-Forward 回测引擎（最优先，决定项目是否"讲科学"）
2. 基础策略（随机/热号/冷号/综合）
3. 收敛曲线 + 理论期望线 + 置信区间 + 排行榜
4. 视觉与交互设计
5. "我的策略"参数调节
6. AI 战报（先模板，后 LLM）
7. Agent Skill 接口（最后接入）

**核心提醒：不要从 AI 开始做。先把"能证明策略是否真的跑赢随机"的回测引擎做对，这是整个项目唯一不能返工的部分；UI、数据源、AI 都可以后续替换或升级。**

---

## 十五、V2 修订记录（基于多方评审）

- 修正 7.5 节 Walk-Forward 回测代码的 off-by-one 索引错误：待预测目标是 `history[t]` 而非 `history[t+1]`。
- 回测中的策略生成不再允许裸用 `Math.random()`：确定性策略用固定排序规则，需要随机成分的策略在回测时必须用「期号+策略名」做种子的伪随机数，保证同一份数据永远得到同一份排行榜。
- 新增 3.5/3.6 节：用配对差值检验和随机模拟分布来比较"策略 vs 随机"，比分别看置信区间更直接、更有说服力。
- 新增蓝球和真实奖级规则、资金归零曲线，作为比"平均命中数"更直观的科普素材。
- 收紧 3.1 节措辞，避免"已被数学证明随机"这类绝对化表述。
- 风险表新增：数据抓取容灾（防云端 IP 被限流）、文案措辞风险。

---

## 十六、V3 修订记录（第三轮评审）

- 新增项目北极星原则："任何策略在没有通过盲测之前，都不准自称有效"，写进方法论页面。
- 明确禁止在生成号码/资金曲线旁放置任何"购买/下单"类按钮或第三方购彩链接。
- 3.5 节补充配对 t 检验的独立性假设讨论，建议用户界面优先展示 3.6 节的蒙特卡洛分位数而非 p 值。
- 3.6 节把"随机基准分布"的方法讲清楚：固定真实开奖序列 + 生成大量独立随机策略，而不是打乱开奖顺序。
- 新增 3.7 节：分层 KPI 体系（基础命中/极端事件/命中分布/资金/统计），避免只用平均命中数做比较。
- "我的策略"拆分为"历史回测成绩"和"盲测成绩"两个数字，防止用户在界面上通过反复调参制造数据窥探。
- 奖级判定与实际奖金金额分离（`prize_level` + `bonus_type: floating/fixed`），避免硬编码固定头奖金额导致资金曲线失真。
- 数据结构加入 `source`/`fetched_at`/版本号字段，用于多数据源交叉核对和排行榜可比性。
- 7.5 节回测引擎入口加入按期号强制升序排序的防御代码，防止数据源"最新在前"的倒序列表导致用未来数据预测过去。
- 第九节重写为 V0→V0.5→V1→V1.5→V2 分阶段路线，第一版明确只做双色球一种彩票，避免范围膨胀成"彩票数据维护工程"。
- 风险表新增：数据倒序陷阱、用户端数据窥探、未验证的第三方脚本/API 地址。
